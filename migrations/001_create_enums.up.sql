-- Enum types for inventory module
-- Generated by backbone-schema

-- Create goods_receipt_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'goods_receipt_status') THEN
        CREATE TYPE goods_receipt_status AS ENUM ('draft', 'receiving', 'partial', 'completed', 'cancelled');
    END IF;
END
$$;

-- Create receipt_item_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'receipt_item_status') THEN
        CREATE TYPE receipt_item_status AS ENUM ('pending', 'received', 'partial', 'rejected', 'returned');
    END IF;
END
$$;

-- Create qc_result enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'qc_result') THEN
        CREATE TYPE qc_result AS ENUM ('pending', 'passed', 'failed', 'conditional');
    END IF;
END
$$;

-- Create product_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'product_type') THEN
        CREATE TYPE product_type AS ENUM ('raw_material', 'finished_good', 'consumable', 'spare_part', 'packaging');
    END IF;
END
$$;

-- Create unit_of_measure enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'unit_of_measure') THEN
        CREATE TYPE unit_of_measure AS ENUM ('pcs', 'kg', 'g', 'liter', 'ml', 'pack', 'box', 'bottle', 'gallon', 'roll', 'sheet');
    END IF;
END
$$;

-- Create costing_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'costing_method') THEN
        CREATE TYPE costing_method AS ENUM ('standard', 'last_purchase', 'weighted_average', 'fifo');
    END IF;
END
$$;

-- Create product_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'product_status') THEN
        CREATE TYPE product_status AS ENUM ('active', 'inactive', 'discontinued');
    END IF;
END
$$;

-- Create purchase_order_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'purchase_order_type') THEN
        CREATE TYPE purchase_order_type AS ENUM ('standard', 'urgent');
    END IF;
END
$$;

-- Create purchase_order_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'purchase_order_status') THEN
        CREATE TYPE purchase_order_status AS ENUM ('draft', 'pending_approval', 'approved', 'rejected', 'sent', 'partial_received', 'received', 'completed', 'cancelled');
    END IF;
END
$$;

-- Create quality_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'quality_status') THEN
        CREATE TYPE quality_status AS ENUM ('pending', 'passed', 'failed', 'conditional');
    END IF;
END
$$;

-- Create adjustment_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'adjustment_type') THEN
        CREATE TYPE adjustment_type AS ENUM ('stock_count', 'damage', 'expiry', 'loss', 'correction');
    END IF;
END
$$;

-- Create adjustment_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'adjustment_status') THEN
        CREATE TYPE adjustment_status AS ENUM ('draft', 'counting', 'verified', 'pending_approval', 'approved', 'rejected', 'posted', 'cancelled');
    END IF;
END
$$;

-- Create batch_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'batch_status') THEN
        CREATE TYPE batch_status AS ENUM ('available', 'quarantine', 'expired', 'depleted');
    END IF;
END
$$;

-- Create movement_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'movement_type') THEN
        CREATE TYPE movement_type AS ENUM ('receipt', 'issue', 'transfer_in', 'transfer_out', 'adjustment_in', 'adjustment_out', 'consumption', 'return_in', 'return_out', 'write_off');
    END IF;
END
$$;

-- Create movement_reason enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'movement_reason') THEN
        CREATE TYPE movement_reason AS ENUM ('purchase', 'sale', 'transfer', 'adjustment', 'damage', 'expiry', 'theft', 'count_variance', 'other');
    END IF;
END
$$;

-- Create movement_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'movement_status') THEN
        CREATE TYPE movement_status AS ENUM ('posted', 'cancelled');
    END IF;
END
$$;

-- Create transfer_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'transfer_type') THEN
        CREATE TYPE transfer_type AS ENUM ('internal', 'inter_provider', 'consignment_out', 'consignment_return');
    END IF;
END
$$;

-- Create transfer_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'transfer_status') THEN
        CREATE TYPE transfer_status AS ENUM ('draft', 'pending_approval', 'approved', 'in_transit', 'partial_received', 'received', 'completed', 'cancelled');
    END IF;
END
$$;

-- Create transfer_item_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'transfer_item_status') THEN
        CREATE TYPE transfer_item_status AS ENUM ('pending', 'shipped', 'partial_received', 'received', 'rejected');
    END IF;
END
$$;

-- Create warehouse_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'warehouse_type') THEN
        CREATE TYPE warehouse_type AS ENUM ('main', 'outlet', 'transit', 'production', 'return_storage', 'quarantine');
    END IF;
END
$$;

-- Create warehouse_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'warehouse_status') THEN
        CREATE TYPE warehouse_status AS ENUM ('active', 'inactive', 'maintenance', 'closed');
    END IF;
END
$$;

