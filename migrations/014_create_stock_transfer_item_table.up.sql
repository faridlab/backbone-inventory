-- Migration: Create stock_transfer_items table
-- Generated by backbone-schema

CREATE TABLE IF NOT EXISTS stock_transfer_items (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    transfer_id UUID NOT NULL,
    product_id UUID NOT NULL,
    requested_quantity NUMERIC NOT NULL,
    shipped_quantity NUMERIC NOT NULL DEFAULT 0,
    received_quantity NUMERIC NOT NULL DEFAULT 0,
    rejected_quantity NUMERIC NOT NULL DEFAULT 0,
    variance_quantity NUMERIC NOT NULL,
    uom TEXT NOT NULL,
    batch_id UUID,
    batch_number TEXT,
    expiry_date DATE,
    unit_cost NUMERIC NOT NULL,
    total_cost NUMERIC NOT NULL,
    from_bin TEXT,
    to_bin TEXT,
    status transfer_item_status NOT NULL DEFAULT 'pending',
    rejection_reason TEXT,
    notes TEXT,
    data JSONB NOT NULL DEFAULT '{}'::jsonb,
    PRIMARY KEY (id)
);

CREATE INDEX IF NOT EXISTS idx_stock_transfer_items_transfer_id_status ON stock_transfer_items (transfer_id, status);

CREATE INDEX IF NOT EXISTS idx_stock_transfer_items_product_id_transfer_id ON stock_transfer_items (product_id, transfer_id);

CREATE INDEX IF NOT EXISTS idx_stock_transfer_items_batch_id ON stock_transfer_items (batch_id) WHERE batch_id IS NOT NULL;

