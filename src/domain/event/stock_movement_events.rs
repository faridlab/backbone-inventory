//! Domain events for StockMovement entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StockMovement;

/// Domain events for StockMovement — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StockMovement>)` variant.
pub type StockMovementEvent = CrudEvent<StockMovement>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StockMovement events.
pub type StockMovementEventPublisher = Arc<dyn CrudEventPublisher<StockMovement> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStockMovementEventPublisher = NoOpCrudEventPublisher<StockMovement>;
