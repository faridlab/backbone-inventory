//! Domain events for Location entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Location;

/// Domain events for Location — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Location>)` variant.
pub type LocationEvent = CrudEvent<Location>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Location events.
pub type LocationEventPublisher = Arc<dyn CrudEventPublisher<Location> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLocationEventPublisher = NoOpCrudEventPublisher<Location>;
