//! Domain events for PackageType entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PackageType;

/// Domain events for PackageType — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PackageType>)` variant.
pub type PackageTypeEvent = CrudEvent<PackageType>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PackageType events.
pub type PackageTypeEventPublisher = Arc<dyn CrudEventPublisher<PackageType> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPackageTypeEventPublisher = NoOpCrudEventPublisher<PackageType>;
