//! Domain events for StockMove entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StockMove;

/// Domain events for StockMove — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StockMove>)` variant.
pub type StockMoveEvent = CrudEvent<StockMove>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StockMove events.
pub type StockMoveEventPublisher = Arc<dyn CrudEventPublisher<StockMove> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStockMoveEventPublisher = NoOpCrudEventPublisher<StockMove>;
