//! Domain events for PutawayRule entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PutawayRule;

/// Domain events for PutawayRule — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PutawayRule>)` variant.
pub type PutawayRuleEvent = CrudEvent<PutawayRule>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PutawayRule events.
pub type PutawayRuleEventPublisher = Arc<dyn CrudEventPublisher<PutawayRule> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPutawayRuleEventPublisher = NoOpCrudEventPublisher<PutawayRule>;
