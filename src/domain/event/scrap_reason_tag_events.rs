//! Domain events for ScrapReasonTag entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ScrapReasonTag;

/// Domain events for ScrapReasonTag — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ScrapReasonTag>)` variant.
pub type ScrapReasonTagEvent = CrudEvent<ScrapReasonTag>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ScrapReasonTag events.
pub type ScrapReasonTagEventPublisher = Arc<dyn CrudEventPublisher<ScrapReasonTag> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopScrapReasonTagEventPublisher = NoOpCrudEventPublisher<ScrapReasonTag>;
