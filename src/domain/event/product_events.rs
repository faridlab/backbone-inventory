//! Domain events for Product entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Product;

/// Domain events for Product — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Product>)` variant.
pub type ProductEvent = CrudEvent<Product>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Product events.
pub type ProductEventPublisher = Arc<dyn CrudEventPublisher<Product> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopProductEventPublisher = NoOpCrudEventPublisher<Product>;
