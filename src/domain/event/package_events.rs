//! Domain events for Package entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Package;

/// Domain events for Package — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Package>)` variant.
pub type PackageEvent = CrudEvent<Package>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Package events.
pub type PackageEventPublisher = Arc<dyn CrudEventPublisher<Package> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPackageEventPublisher = NoOpCrudEventPublisher<Package>;
