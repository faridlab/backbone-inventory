//! Domain events for Bin entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Bin;

/// Domain events for Bin — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Bin>)` variant.
pub type BinEvent = CrudEvent<Bin>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Bin events.
pub type BinEventPublisher = Arc<dyn CrudEventPublisher<Bin> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopBinEventPublisher = NoOpCrudEventPublisher<Bin>;
