//! Domain events for PickingBatch entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PickingBatch;

/// Domain events for PickingBatch — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PickingBatch>)` variant.
pub type PickingBatchEvent = CrudEvent<PickingBatch>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PickingBatch events.
pub type PickingBatchEventPublisher = Arc<dyn CrudEventPublisher<PickingBatch> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPickingBatchEventPublisher = NoOpCrudEventPublisher<PickingBatch>;
