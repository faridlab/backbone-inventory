//! Domain events for LandedCostAdjustmentLine entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::LandedCostAdjustmentLine;

/// Domain events for LandedCostAdjustmentLine — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<LandedCostAdjustmentLine>)` variant.
pub type LandedCostAdjustmentLineEvent = CrudEvent<LandedCostAdjustmentLine>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for LandedCostAdjustmentLine events.
pub type LandedCostAdjustmentLineEventPublisher = Arc<dyn CrudEventPublisher<LandedCostAdjustmentLine> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLandedCostAdjustmentLineEventPublisher = NoOpCrudEventPublisher<LandedCostAdjustmentLine>;
