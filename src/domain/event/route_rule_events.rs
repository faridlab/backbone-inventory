//! Domain events for RouteRule entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RouteRule;

/// Domain events for RouteRule — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RouteRule>)` variant.
pub type RouteRuleEvent = CrudEvent<RouteRule>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RouteRule events.
pub type RouteRuleEventPublisher = Arc<dyn CrudEventPublisher<RouteRule> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRouteRuleEventPublisher = NoOpCrudEventPublisher<RouteRule>;
