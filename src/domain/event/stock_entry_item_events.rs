//! Domain events for StockEntryItem entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StockEntryItem;

/// Domain events for StockEntryItem — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StockEntryItem>)` variant.
pub type StockEntryItemEvent = CrudEvent<StockEntryItem>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StockEntryItem events.
pub type StockEntryItemEventPublisher = Arc<dyn CrudEventPublisher<StockEntryItem> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStockEntryItemEventPublisher = NoOpCrudEventPublisher<StockEntryItem>;
