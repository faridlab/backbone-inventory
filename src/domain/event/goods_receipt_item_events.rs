//! Domain events for GoodsReceiptItem entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::GoodsReceiptItem;

/// Domain events for GoodsReceiptItem — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<GoodsReceiptItem>)` variant.
pub type GoodsReceiptItemEvent = CrudEvent<GoodsReceiptItem>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for GoodsReceiptItem events.
pub type GoodsReceiptItemEventPublisher = Arc<dyn CrudEventPublisher<GoodsReceiptItem> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopGoodsReceiptItemEventPublisher = NoOpCrudEventPublisher<GoodsReceiptItem>;
