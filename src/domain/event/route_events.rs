//! Domain events for Route entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Route;

/// Domain events for Route — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Route>)` variant.
pub type RouteEvent = CrudEvent<Route>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Route events.
pub type RouteEventPublisher = Arc<dyn CrudEventPublisher<Route> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRouteEventPublisher = NoOpCrudEventPublisher<Route>;
