//! Domain events for Scrap entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Scrap;

/// Domain events for Scrap — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Scrap>)` variant.
pub type ScrapEvent = CrudEvent<Scrap>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Scrap events.
pub type ScrapEventPublisher = Arc<dyn CrudEventPublisher<Scrap> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopScrapEventPublisher = NoOpCrudEventPublisher<Scrap>;
