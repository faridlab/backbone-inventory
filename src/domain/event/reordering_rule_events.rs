//! Domain events for ReorderingRule entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ReorderingRule;

/// Domain events for ReorderingRule — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ReorderingRule>)` variant.
pub type ReorderingRuleEvent = CrudEvent<ReorderingRule>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ReorderingRule events.
pub type ReorderingRuleEventPublisher = Arc<dyn CrudEventPublisher<ReorderingRule> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopReorderingRuleEventPublisher = NoOpCrudEventPublisher<ReorderingRule>;
