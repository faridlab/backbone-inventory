//! Domain events for StockItem entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StockItem;

/// Domain events for StockItem — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StockItem>)` variant.
pub type StockItemEvent = CrudEvent<StockItem>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StockItem events.
pub type StockItemEventPublisher = Arc<dyn CrudEventPublisher<StockItem> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStockItemEventPublisher = NoOpCrudEventPublisher<StockItem>;
