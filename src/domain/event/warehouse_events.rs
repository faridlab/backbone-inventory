//! Domain events for Warehouse entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Warehouse;

/// Domain events for Warehouse — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Warehouse>)` variant.
pub type WarehouseEvent = CrudEvent<Warehouse>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Warehouse events.
pub type WarehouseEventPublisher = Arc<dyn CrudEventPublisher<Warehouse> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopWarehouseEventPublisher = NoOpCrudEventPublisher<Warehouse>;
