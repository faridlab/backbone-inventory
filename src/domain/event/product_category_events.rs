//! Domain events for ProductCategory entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ProductCategory;

/// Domain events for ProductCategory — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ProductCategory>)` variant.
pub type ProductCategoryEvent = CrudEvent<ProductCategory>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ProductCategory events.
pub type ProductCategoryEventPublisher = Arc<dyn CrudEventPublisher<ProductCategory> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopProductCategoryEventPublisher = NoOpCrudEventPublisher<ProductCategory>;
