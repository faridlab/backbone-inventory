//! Domain events for DeliveryNote entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::DeliveryNote;

/// Domain events for DeliveryNote — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<DeliveryNote>)` variant.
pub type DeliveryNoteEvent = CrudEvent<DeliveryNote>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for DeliveryNote events.
pub type DeliveryNoteEventPublisher = Arc<dyn CrudEventPublisher<DeliveryNote> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopDeliveryNoteEventPublisher = NoOpCrudEventPublisher<DeliveryNote>;
