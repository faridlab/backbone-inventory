//! Domain events for StockLedgerEntry entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StockLedgerEntry;

/// Domain events for StockLedgerEntry — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StockLedgerEntry>)` variant.
pub type StockLedgerEntryEvent = CrudEvent<StockLedgerEntry>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StockLedgerEntry events.
pub type StockLedgerEntryEventPublisher = Arc<dyn CrudEventPublisher<StockLedgerEntry> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStockLedgerEntryEventPublisher = NoOpCrudEventPublisher<StockLedgerEntry>;
