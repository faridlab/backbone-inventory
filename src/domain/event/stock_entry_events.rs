//! Domain events for StockEntry entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::StockEntry;

/// Domain events for StockEntry — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<StockEntry>)` variant.
pub type StockEntryEvent = CrudEvent<StockEntry>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for StockEntry events.
pub type StockEntryEventPublisher = Arc<dyn CrudEventPublisher<StockEntry> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStockEntryEventPublisher = NoOpCrudEventPublisher<StockEntry>;
