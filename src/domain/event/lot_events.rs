//! Domain events for Lot entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Lot;

/// Domain events for Lot — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Lot>)` variant.
pub type LotEvent = CrudEvent<Lot>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Lot events.
pub type LotEventPublisher = Arc<dyn CrudEventPublisher<Lot> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLotEventPublisher = NoOpCrudEventPublisher<Lot>;
