//! Repository trait for LandedCost aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LandedCost aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{LandedCost, GlPostingState, LandedCostState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LandedCostPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LandedCostPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LandedCostPaginatedResult {
    pub data: Vec<LandedCost>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LandedCostFilter {
    pub lc_number: Option<String>,
    pub branch_id: Option<Uuid>,
    pub target_receipt_id: Option<Uuid>,
    pub transfer_id: Option<Uuid>,
    pub currency: Option<String>,
    pub state: Option<LandedCostState>,
    pub posting_state: Option<GlPostingState>,
    pub journal_id: Option<Uuid>,
    pub accounting_post_id: Option<Uuid>,
    pub notes: Option<String>,
}

impl LandedCostFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.lc_number.is_some() || self.branch_id.is_some() || self.target_receipt_id.is_some() || self.transfer_id.is_some() || self.currency.is_some() || self.state.is_some() || self.posting_state.is_some() || self.journal_id.is_some() || self.accounting_post_id.is_some() || self.notes.is_some()
    }
}

/// Repository trait for LandedCost aggregate
///
/// This trait defines the persistence contract for LandedCost entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LandedCostRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new landed_cost entity
    async fn save(&self, entity: &LandedCost) -> Result<LandedCost>;

    /// Find landed_cost by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LandedCost>>;

    /// Find all landed_cost entities
    async fn find_all(&self) -> Result<Vec<LandedCost>>;

    /// Update landed_cost by ID
    async fn update(&self, id: &str, entity: &LandedCost) -> Result<Option<LandedCost>>;

    /// Delete landed_cost by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List landed_cost with pagination
    async fn list(&self, params: LandedCostPaginationParams) -> Result<LandedCostPaginatedResult>;

    /// List landed_cost with pagination and filters
    async fn list_with_filters(&self, params: LandedCostPaginationParams, filters: LandedCostFilter) -> Result<LandedCostPaginatedResult>;

    /// Count all landed_cost entities
    async fn count(&self) -> Result<u64>;

    /// Count landed_cost entities matching filters
    async fn count_with_filters(&self, filters: LandedCostFilter) -> Result<u64>;

    /// Check if landed_cost exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete landed_cost by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted landed_cost by ID
    async fn restore(&self, id: &str) -> Result<Option<LandedCost>>;

    /// List soft-deleted landed_cost entities
    async fn list_deleted(&self, params: LandedCostPaginationParams) -> Result<LandedCostPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save landed_cost entities
    async fn bulk_save(&self, entities: &[LandedCost]) -> Result<Vec<LandedCost>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
