//! Repository trait for LandedCostAdjustmentLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LandedCostAdjustmentLine aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::LandedCostAdjustmentLine;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LandedCostAdjustmentLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LandedCostAdjustmentLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LandedCostAdjustmentLinePaginatedResult {
    pub data: Vec<LandedCostAdjustmentLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LandedCostAdjustmentLineFilter {
    pub lc_id: Option<Uuid>,
    pub move_line_id: Option<Uuid>,
    pub cost_line_id: Option<Uuid>,
}

impl LandedCostAdjustmentLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.lc_id.is_some() || self.move_line_id.is_some() || self.cost_line_id.is_some()
    }
}

/// Repository trait for LandedCostAdjustmentLine aggregate
///
/// This trait defines the persistence contract for LandedCostAdjustmentLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LandedCostAdjustmentLineRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new landed_cost_adjustment_line entity
    async fn save(&self, entity: &LandedCostAdjustmentLine) -> Result<LandedCostAdjustmentLine>;

    /// Find landed_cost_adjustment_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LandedCostAdjustmentLine>>;

    /// Find all landed_cost_adjustment_line entities
    async fn find_all(&self) -> Result<Vec<LandedCostAdjustmentLine>>;

    /// Update landed_cost_adjustment_line by ID
    async fn update(&self, id: &str, entity: &LandedCostAdjustmentLine) -> Result<Option<LandedCostAdjustmentLine>>;

    /// Delete landed_cost_adjustment_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List landed_cost_adjustment_line with pagination
    async fn list(&self, params: LandedCostAdjustmentLinePaginationParams) -> Result<LandedCostAdjustmentLinePaginatedResult>;

    /// List landed_cost_adjustment_line with pagination and filters
    async fn list_with_filters(&self, params: LandedCostAdjustmentLinePaginationParams, filters: LandedCostAdjustmentLineFilter) -> Result<LandedCostAdjustmentLinePaginatedResult>;

    /// Count all landed_cost_adjustment_line entities
    async fn count(&self) -> Result<u64>;

    /// Count landed_cost_adjustment_line entities matching filters
    async fn count_with_filters(&self, filters: LandedCostAdjustmentLineFilter) -> Result<u64>;

    /// Check if landed_cost_adjustment_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete landed_cost_adjustment_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted landed_cost_adjustment_line by ID
    async fn restore(&self, id: &str) -> Result<Option<LandedCostAdjustmentLine>>;

    /// List soft-deleted landed_cost_adjustment_line entities
    async fn list_deleted(&self, params: LandedCostAdjustmentLinePaginationParams) -> Result<LandedCostAdjustmentLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save landed_cost_adjustment_line entities
    async fn bulk_save(&self, entities: &[LandedCostAdjustmentLine]) -> Result<Vec<LandedCostAdjustmentLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
