//! Repository trait for StockMove aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockMove aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockMove, MoveState, Priority, ProcureMethod};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockMovePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockMovePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockMovePaginatedResult {
    pub data: Vec<StockMove>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockMoveFilter {
    pub name: Option<String>,
    pub state: Option<MoveState>,
    pub priority: Option<Priority>,
    pub item_id: Option<Uuid>,
    pub procure_method: Option<ProcureMethod>,
    pub picking_id: Option<Uuid>,
    pub origin: Option<String>,
    pub location_id: Option<Uuid>,
    pub location_dest_id: Option<Uuid>,
    pub partner_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub rule_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub orderpoint_id: Option<Uuid>,
    pub is_inventory: Option<bool>,
    pub scrapped: Option<bool>,
    pub propagate_cancel: Option<bool>,
}

impl StockMoveFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.state.is_some() || self.priority.is_some() || self.item_id.is_some() || self.procure_method.is_some() || self.picking_id.is_some() || self.origin.is_some() || self.location_id.is_some() || self.location_dest_id.is_some() || self.partner_id.is_some() || self.company_id.is_some() || self.rule_id.is_some() || self.warehouse_id.is_some() || self.orderpoint_id.is_some() || self.is_inventory.is_some() || self.scrapped.is_some() || self.propagate_cancel.is_some()
    }
}

/// Repository trait for StockMove aggregate
///
/// This trait defines the persistence contract for StockMove entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockMoveRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_move entity
    async fn save(&self, entity: &StockMove) -> Result<StockMove>;

    /// Find stock_move by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockMove>>;

    /// Find all stock_move entities
    async fn find_all(&self) -> Result<Vec<StockMove>>;

    /// Update stock_move by ID
    async fn update(&self, id: &str, entity: &StockMove) -> Result<Option<StockMove>>;

    /// Delete stock_move by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_move with pagination
    async fn list(&self, params: StockMovePaginationParams) -> Result<StockMovePaginatedResult>;

    /// List stock_move with pagination and filters
    async fn list_with_filters(&self, params: StockMovePaginationParams, filters: StockMoveFilter) -> Result<StockMovePaginatedResult>;

    /// Count all stock_move entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_move entities matching filters
    async fn count_with_filters(&self, filters: StockMoveFilter) -> Result<u64>;

    /// Check if stock_move exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_move by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_move by ID
    async fn restore(&self, id: &str) -> Result<Option<StockMove>>;

    /// List soft-deleted stock_move entities
    async fn list_deleted(&self, params: StockMovePaginationParams) -> Result<StockMovePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_move entities
    async fn bulk_save(&self, entities: &[StockMove]) -> Result<Vec<StockMove>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
