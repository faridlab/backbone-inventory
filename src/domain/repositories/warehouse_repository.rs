//! Repository trait for Warehouse aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Warehouse aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Warehouse, WarehouseStatus, WarehouseType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WarehousePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl WarehousePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct WarehousePaginatedResult {
    pub data: Vec<Warehouse>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WarehouseFilter {
    pub provider_id: Option<Uuid>,
    pub outlet_id: Option<Uuid>,
    pub code: Option<String>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub warehouse_type: Option<WarehouseType>,
    pub is_main: Option<bool>,
    pub address_id: Option<Uuid>,
    pub contact_name: Option<String>,
    pub contact_email: Option<String>,
    pub capacity_unit: Option<String>,
    pub allow_receipt: Option<bool>,
    pub allow_issue: Option<bool>,
    pub allow_transfer_in: Option<bool>,
    pub allow_transfer_out: Option<bool>,
    pub allow_adjustment: Option<bool>,
    pub default_inventory_account_id: Option<Uuid>,
    pub status: Option<WarehouseStatus>,
    pub notes: Option<String>,
}

impl WarehouseFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.outlet_id.is_some() || self.code.is_some() || self.name.is_some() || self.description.is_some() || self.warehouse_type.is_some() || self.is_main.is_some() || self.address_id.is_some() || self.contact_name.is_some() || self.contact_email.is_some() || self.capacity_unit.is_some() || self.allow_receipt.is_some() || self.allow_issue.is_some() || self.allow_transfer_in.is_some() || self.allow_transfer_out.is_some() || self.allow_adjustment.is_some() || self.default_inventory_account_id.is_some() || self.status.is_some() || self.notes.is_some()
    }
}

/// Repository trait for Warehouse aggregate
///
/// This trait defines the persistence contract for Warehouse entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WarehouseRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new warehouse entity
    async fn save(&self, entity: &Warehouse) -> Result<Warehouse>;

    /// Find warehouse by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Warehouse>>;

    /// Find all warehouse entities
    async fn find_all(&self) -> Result<Vec<Warehouse>>;

    /// Update warehouse by ID
    async fn update(&self, id: &str, entity: &Warehouse) -> Result<Option<Warehouse>>;

    /// Delete warehouse by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List warehouse with pagination
    async fn list(&self, params: WarehousePaginationParams) -> Result<WarehousePaginatedResult>;

    /// List warehouse with pagination and filters
    async fn list_with_filters(&self, params: WarehousePaginationParams, filters: WarehouseFilter) -> Result<WarehousePaginatedResult>;

    /// Count all warehouse entities
    async fn count(&self) -> Result<u64>;

    /// Count warehouse entities matching filters
    async fn count_with_filters(&self, filters: WarehouseFilter) -> Result<u64>;

    /// Check if warehouse exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete warehouse by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted warehouse by ID
    async fn restore(&self, id: &str) -> Result<Option<Warehouse>>;

    /// List soft-deleted warehouse entities
    async fn list_deleted(&self, params: WarehousePaginationParams) -> Result<WarehousePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save warehouse entities
    async fn bulk_save(&self, entities: &[Warehouse]) -> Result<Vec<Warehouse>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
