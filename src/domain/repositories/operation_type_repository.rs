//! Repository trait for OperationType aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the OperationType aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{OperationType, CreateBackorder, MoveType, PickingCode, ReservationMethod};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OperationTypePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl OperationTypePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct OperationTypePaginatedResult {
    pub data: Vec<OperationType>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OperationTypeFilter {
    pub name: Option<String>,
    pub sequence_code: Option<String>,
    pub code: Option<PickingCode>,
    pub active: Option<bool>,
    pub company_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub default_location_src_id: Option<Uuid>,
    pub default_location_dest_id: Option<Uuid>,
    pub reservation_method: Option<ReservationMethod>,
    pub move_type: Option<MoveType>,
    pub create_backorder: Option<CreateBackorder>,
    pub use_create_lots: Option<bool>,
    pub use_existing_lots: Option<bool>,
}

impl OperationTypeFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.sequence_code.is_some() || self.code.is_some() || self.active.is_some() || self.company_id.is_some() || self.warehouse_id.is_some() || self.default_location_src_id.is_some() || self.default_location_dest_id.is_some() || self.reservation_method.is_some() || self.move_type.is_some() || self.create_backorder.is_some() || self.use_create_lots.is_some() || self.use_existing_lots.is_some()
    }
}

/// Repository trait for OperationType aggregate
///
/// This trait defines the persistence contract for OperationType entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait OperationTypeRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new operation_type entity
    async fn save(&self, entity: &OperationType) -> Result<OperationType>;

    /// Find operation_type by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<OperationType>>;

    /// Find all operation_type entities
    async fn find_all(&self) -> Result<Vec<OperationType>>;

    /// Update operation_type by ID
    async fn update(&self, id: &str, entity: &OperationType) -> Result<Option<OperationType>>;

    /// Delete operation_type by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List operation_type with pagination
    async fn list(&self, params: OperationTypePaginationParams) -> Result<OperationTypePaginatedResult>;

    /// List operation_type with pagination and filters
    async fn list_with_filters(&self, params: OperationTypePaginationParams, filters: OperationTypeFilter) -> Result<OperationTypePaginatedResult>;

    /// Count all operation_type entities
    async fn count(&self) -> Result<u64>;

    /// Count operation_type entities matching filters
    async fn count_with_filters(&self, filters: OperationTypeFilter) -> Result<u64>;

    /// Check if operation_type exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete operation_type by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted operation_type by ID
    async fn restore(&self, id: &str) -> Result<Option<OperationType>>;

    /// List soft-deleted operation_type entities
    async fn list_deleted(&self, params: OperationTypePaginationParams) -> Result<OperationTypePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save operation_type entities
    async fn bulk_save(&self, entities: &[OperationType]) -> Result<Vec<OperationType>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
