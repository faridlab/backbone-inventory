//! Repository trait for StockTransfer aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockTransfer aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockTransfer, TransferStatus, TransferType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockTransferPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockTransferPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockTransferPaginatedResult {
    pub data: Vec<StockTransfer>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockTransferFilter {
    pub provider_id: Option<Uuid>,
    pub transfer_number: Option<String>,
    pub transfer_type: Option<TransferType>,
    pub from_warehouse_id: Option<Uuid>,
    pub from_location: Option<String>,
    pub to_warehouse_id: Option<Uuid>,
    pub to_location: Option<String>,
    pub to_provider_id: Option<Uuid>,
    pub currency: Option<String>,
    pub shipping_method: Option<String>,
    pub carrier: Option<String>,
    pub tracking_number: Option<String>,
    pub status: Option<TransferStatus>,
    pub requires_approval: Option<bool>,
    pub approved_by: Option<Uuid>,
    pub requested_by: Option<Uuid>,
    pub shipped_by: Option<Uuid>,
    pub received_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notes: Option<String>,
    pub receiving_notes: Option<String>,
}

impl StockTransferFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.transfer_number.is_some() || self.transfer_type.is_some() || self.from_warehouse_id.is_some() || self.from_location.is_some() || self.to_warehouse_id.is_some() || self.to_location.is_some() || self.to_provider_id.is_some() || self.currency.is_some() || self.shipping_method.is_some() || self.carrier.is_some() || self.tracking_number.is_some() || self.status.is_some() || self.requires_approval.is_some() || self.approved_by.is_some() || self.requested_by.is_some() || self.shipped_by.is_some() || self.received_by.is_some() || self.reason.is_some() || self.notes.is_some() || self.receiving_notes.is_some()
    }
}

/// Repository trait for StockTransfer aggregate
///
/// This trait defines the persistence contract for StockTransfer entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockTransferRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_transfer entity
    async fn save(&self, entity: &StockTransfer) -> Result<StockTransfer>;

    /// Find stock_transfer by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockTransfer>>;

    /// Find all stock_transfer entities
    async fn find_all(&self) -> Result<Vec<StockTransfer>>;

    /// Update stock_transfer by ID
    async fn update(&self, id: &str, entity: &StockTransfer) -> Result<Option<StockTransfer>>;

    /// Delete stock_transfer by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_transfer with pagination
    async fn list(&self, params: StockTransferPaginationParams) -> Result<StockTransferPaginatedResult>;

    /// List stock_transfer with pagination and filters
    async fn list_with_filters(&self, params: StockTransferPaginationParams, filters: StockTransferFilter) -> Result<StockTransferPaginatedResult>;

    /// Count all stock_transfer entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_transfer entities matching filters
    async fn count_with_filters(&self, filters: StockTransferFilter) -> Result<u64>;

    /// Check if stock_transfer exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_transfer by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_transfer by ID
    async fn restore(&self, id: &str) -> Result<Option<StockTransfer>>;

    /// List soft-deleted stock_transfer entities
    async fn list_deleted(&self, params: StockTransferPaginationParams) -> Result<StockTransferPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_transfer entities
    async fn bulk_save(&self, entities: &[StockTransfer]) -> Result<Vec<StockTransfer>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
