//! Repository trait for ProductCategory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ProductCategory aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ProductCategory, CostingMethod, UnitOfMeasure};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProductCategoryPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ProductCategoryPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ProductCategoryPaginatedResult {
    pub data: Vec<ProductCategory>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProductCategoryFilter {
    pub provider_id: Option<Uuid>,
    pub code: Option<String>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub parent_id: Option<Uuid>,
    pub path: Option<String>,
    pub default_uom: Option<UnitOfMeasure>,
    pub default_costing_method: Option<CostingMethod>,
    pub default_inventory_account_id: Option<Uuid>,
    pub default_cogs_account_id: Option<Uuid>,
    pub is_active: Option<bool>,
}

impl ProductCategoryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.code.is_some() || self.name.is_some() || self.description.is_some() || self.parent_id.is_some() || self.path.is_some() || self.default_uom.is_some() || self.default_costing_method.is_some() || self.default_inventory_account_id.is_some() || self.default_cogs_account_id.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for ProductCategory aggregate
///
/// This trait defines the persistence contract for ProductCategory entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ProductCategoryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new product_category entity
    async fn save(&self, entity: &ProductCategory) -> Result<ProductCategory>;

    /// Find product_category by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ProductCategory>>;

    /// Find all product_category entities
    async fn find_all(&self) -> Result<Vec<ProductCategory>>;

    /// Update product_category by ID
    async fn update(&self, id: &str, entity: &ProductCategory) -> Result<Option<ProductCategory>>;

    /// Delete product_category by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List product_category with pagination
    async fn list(&self, params: ProductCategoryPaginationParams) -> Result<ProductCategoryPaginatedResult>;

    /// List product_category with pagination and filters
    async fn list_with_filters(&self, params: ProductCategoryPaginationParams, filters: ProductCategoryFilter) -> Result<ProductCategoryPaginatedResult>;

    /// Count all product_category entities
    async fn count(&self) -> Result<u64>;

    /// Count product_category entities matching filters
    async fn count_with_filters(&self, filters: ProductCategoryFilter) -> Result<u64>;

    /// Check if product_category exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete product_category by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted product_category by ID
    async fn restore(&self, id: &str) -> Result<Option<ProductCategory>>;

    /// List soft-deleted product_category entities
    async fn list_deleted(&self, params: ProductCategoryPaginationParams) -> Result<ProductCategoryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save product_category entities
    async fn bulk_save(&self, entities: &[ProductCategory]) -> Result<Vec<ProductCategory>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
