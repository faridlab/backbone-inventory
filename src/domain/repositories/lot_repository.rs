//! Repository trait for Lot aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Lot aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::Lot;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LotPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LotPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LotPaginatedResult {
    pub data: Vec<Lot>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LotFilter {
    pub name: Option<String>,
    pub reference: Option<String>,
    pub item_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub location_id: Option<Uuid>,
    pub note: Option<String>,
}

impl LotFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.reference.is_some() || self.item_id.is_some() || self.company_id.is_some() || self.location_id.is_some() || self.note.is_some()
    }
}

/// Repository trait for Lot aggregate
///
/// This trait defines the persistence contract for Lot entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LotRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new lot entity
    async fn save(&self, entity: &Lot) -> Result<Lot>;

    /// Find lot by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Lot>>;

    /// Find all lot entities
    async fn find_all(&self) -> Result<Vec<Lot>>;

    /// Update lot by ID
    async fn update(&self, id: &str, entity: &Lot) -> Result<Option<Lot>>;

    /// Delete lot by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List lot with pagination
    async fn list(&self, params: LotPaginationParams) -> Result<LotPaginatedResult>;

    /// List lot with pagination and filters
    async fn list_with_filters(&self, params: LotPaginationParams, filters: LotFilter) -> Result<LotPaginatedResult>;

    /// Count all lot entities
    async fn count(&self) -> Result<u64>;

    /// Count lot entities matching filters
    async fn count_with_filters(&self, filters: LotFilter) -> Result<u64>;

    /// Check if lot exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete lot by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted lot by ID
    async fn restore(&self, id: &str) -> Result<Option<Lot>>;

    /// List soft-deleted lot entities
    async fn list_deleted(&self, params: LotPaginationParams) -> Result<LotPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save lot entities
    async fn bulk_save(&self, entities: &[Lot]) -> Result<Vec<Lot>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
