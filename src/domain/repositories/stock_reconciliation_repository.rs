//! Repository trait for StockReconciliation aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockReconciliation aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockReconciliation, DocStatus, GlPostingState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockReconciliationPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockReconciliationPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockReconciliationPaginatedResult {
    pub data: Vec<StockReconciliation>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockReconciliationFilter {
    pub recon_number: Option<String>,
    pub company_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub inventory_account_id: Option<Uuid>,
    pub adjustment_account_id: Option<Uuid>,
    pub status: Option<DocStatus>,
    pub posting_state: Option<GlPostingState>,
    pub journal_id: Option<Uuid>,
    pub accounting_post_id: Option<Uuid>,
}

impl StockReconciliationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.recon_number.is_some() || self.company_id.is_some() || self.warehouse_id.is_some() || self.inventory_account_id.is_some() || self.adjustment_account_id.is_some() || self.status.is_some() || self.posting_state.is_some() || self.journal_id.is_some() || self.accounting_post_id.is_some()
    }
}

/// Repository trait for StockReconciliation aggregate
///
/// This trait defines the persistence contract for StockReconciliation entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockReconciliationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_reconciliation entity
    async fn save(&self, entity: &StockReconciliation) -> Result<StockReconciliation>;

    /// Find stock_reconciliation by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockReconciliation>>;

    /// Find all stock_reconciliation entities
    async fn find_all(&self) -> Result<Vec<StockReconciliation>>;

    /// Update stock_reconciliation by ID
    async fn update(&self, id: &str, entity: &StockReconciliation) -> Result<Option<StockReconciliation>>;

    /// Delete stock_reconciliation by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_reconciliation with pagination
    async fn list(&self, params: StockReconciliationPaginationParams) -> Result<StockReconciliationPaginatedResult>;

    /// List stock_reconciliation with pagination and filters
    async fn list_with_filters(&self, params: StockReconciliationPaginationParams, filters: StockReconciliationFilter) -> Result<StockReconciliationPaginatedResult>;

    /// Count all stock_reconciliation entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_reconciliation entities matching filters
    async fn count_with_filters(&self, filters: StockReconciliationFilter) -> Result<u64>;

    /// Check if stock_reconciliation exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_reconciliation by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_reconciliation by ID
    async fn restore(&self, id: &str) -> Result<Option<StockReconciliation>>;

    /// List soft-deleted stock_reconciliation entities
    async fn list_deleted(&self, params: StockReconciliationPaginationParams) -> Result<StockReconciliationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_reconciliation entities
    async fn bulk_save(&self, entities: &[StockReconciliation]) -> Result<Vec<StockReconciliation>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
