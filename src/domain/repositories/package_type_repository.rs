//! Repository trait for PackageType aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PackageType aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PackageType, PackageUse};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PackageTypePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PackageTypePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PackageTypePaginatedResult {
    pub data: Vec<PackageType>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PackageTypeFilter {
    pub name: Option<String>,
    pub barcode: Option<String>,
    pub package_use: Option<PackageUse>,
    pub active: Option<bool>,
    pub company_id: Option<Uuid>,
}

impl PackageTypeFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.barcode.is_some() || self.package_use.is_some() || self.active.is_some() || self.company_id.is_some()
    }
}

/// Repository trait for PackageType aggregate
///
/// This trait defines the persistence contract for PackageType entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PackageTypeRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new package_type entity
    async fn save(&self, entity: &PackageType) -> Result<PackageType>;

    /// Find package_type by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PackageType>>;

    /// Find all package_type entities
    async fn find_all(&self) -> Result<Vec<PackageType>>;

    /// Update package_type by ID
    async fn update(&self, id: &str, entity: &PackageType) -> Result<Option<PackageType>>;

    /// Delete package_type by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List package_type with pagination
    async fn list(&self, params: PackageTypePaginationParams) -> Result<PackageTypePaginatedResult>;

    /// List package_type with pagination and filters
    async fn list_with_filters(&self, params: PackageTypePaginationParams, filters: PackageTypeFilter) -> Result<PackageTypePaginatedResult>;

    /// Count all package_type entities
    async fn count(&self) -> Result<u64>;

    /// Count package_type entities matching filters
    async fn count_with_filters(&self, filters: PackageTypeFilter) -> Result<u64>;

    /// Check if package_type exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete package_type by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted package_type by ID
    async fn restore(&self, id: &str) -> Result<Option<PackageType>>;

    /// List soft-deleted package_type entities
    async fn list_deleted(&self, params: PackageTypePaginationParams) -> Result<PackageTypePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save package_type entities
    async fn bulk_save(&self, entities: &[PackageType]) -> Result<Vec<PackageType>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
