//! Repository trait for Package aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Package aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::Package;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PackagePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PackagePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PackagePaginatedResult {
    pub data: Vec<Package>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PackageFilter {
    pub name: Option<String>,
    pub complete_name: Option<String>,
    pub location_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub parent_package_id: Option<Uuid>,
    pub parent_path: Option<String>,
    pub package_type_id: Option<Uuid>,
}

impl PackageFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.complete_name.is_some() || self.location_id.is_some() || self.company_id.is_some() || self.parent_package_id.is_some() || self.parent_path.is_some() || self.package_type_id.is_some()
    }
}

/// Repository trait for Package aggregate
///
/// This trait defines the persistence contract for Package entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PackageRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new package entity
    async fn save(&self, entity: &Package) -> Result<Package>;

    /// Find package by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Package>>;

    /// Find all package entities
    async fn find_all(&self) -> Result<Vec<Package>>;

    /// Update package by ID
    async fn update(&self, id: &str, entity: &Package) -> Result<Option<Package>>;

    /// Delete package by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List package with pagination
    async fn list(&self, params: PackagePaginationParams) -> Result<PackagePaginatedResult>;

    /// List package with pagination and filters
    async fn list_with_filters(&self, params: PackagePaginationParams, filters: PackageFilter) -> Result<PackagePaginatedResult>;

    /// Count all package entities
    async fn count(&self) -> Result<u64>;

    /// Count package entities matching filters
    async fn count_with_filters(&self, filters: PackageFilter) -> Result<u64>;

    /// Check if package exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete package by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted package by ID
    async fn restore(&self, id: &str) -> Result<Option<Package>>;

    /// List soft-deleted package entities
    async fn list_deleted(&self, params: PackagePaginationParams) -> Result<PackagePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save package entities
    async fn bulk_save(&self, entities: &[Package]) -> Result<Vec<Package>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
