//! Repository trait for StorageCategoryCapacity aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StorageCategoryCapacity aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::StorageCategoryCapacity;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StorageCategoryCapacityPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StorageCategoryCapacityPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StorageCategoryCapacityPaginatedResult {
    pub data: Vec<StorageCategoryCapacity>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StorageCategoryCapacityFilter {
    pub storage_category_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
    pub package_type_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
}

impl StorageCategoryCapacityFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.storage_category_id.is_some() || self.item_id.is_some() || self.package_type_id.is_some() || self.company_id.is_some()
    }
}

/// Repository trait for StorageCategoryCapacity aggregate
///
/// This trait defines the persistence contract for StorageCategoryCapacity entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StorageCategoryCapacityRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new storage_category_capacity entity
    async fn save(&self, entity: &StorageCategoryCapacity) -> Result<StorageCategoryCapacity>;

    /// Find storage_category_capacity by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StorageCategoryCapacity>>;

    /// Find all storage_category_capacity entities
    async fn find_all(&self) -> Result<Vec<StorageCategoryCapacity>>;

    /// Update storage_category_capacity by ID
    async fn update(&self, id: &str, entity: &StorageCategoryCapacity) -> Result<Option<StorageCategoryCapacity>>;

    /// Delete storage_category_capacity by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List storage_category_capacity with pagination
    async fn list(&self, params: StorageCategoryCapacityPaginationParams) -> Result<StorageCategoryCapacityPaginatedResult>;

    /// List storage_category_capacity with pagination and filters
    async fn list_with_filters(&self, params: StorageCategoryCapacityPaginationParams, filters: StorageCategoryCapacityFilter) -> Result<StorageCategoryCapacityPaginatedResult>;

    /// Count all storage_category_capacity entities
    async fn count(&self) -> Result<u64>;

    /// Count storage_category_capacity entities matching filters
    async fn count_with_filters(&self, filters: StorageCategoryCapacityFilter) -> Result<u64>;

    /// Check if storage_category_capacity exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete storage_category_capacity by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted storage_category_capacity by ID
    async fn restore(&self, id: &str) -> Result<Option<StorageCategoryCapacity>>;

    /// List soft-deleted storage_category_capacity entities
    async fn list_deleted(&self, params: StorageCategoryCapacityPaginationParams) -> Result<StorageCategoryCapacityPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save storage_category_capacity entities
    async fn bulk_save(&self, entities: &[StorageCategoryCapacity]) -> Result<Vec<StorageCategoryCapacity>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
