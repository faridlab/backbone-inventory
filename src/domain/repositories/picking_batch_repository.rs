//! Repository trait for PickingBatch aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PickingBatch aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PickingBatch, PickingBatchState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PickingBatchPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PickingBatchPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PickingBatchPaginatedResult {
    pub data: Vec<PickingBatch>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PickingBatchFilter {
    pub name: Option<String>,
    pub note: Option<String>,
    pub state: Option<PickingBatchState>,
    pub is_wave: Option<bool>,
    pub user_id: Option<Uuid>,
    pub had_members: Option<bool>,
    pub company_id: Option<Uuid>,
}

impl PickingBatchFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.note.is_some() || self.state.is_some() || self.is_wave.is_some() || self.user_id.is_some() || self.had_members.is_some() || self.company_id.is_some()
    }
}

/// Repository trait for PickingBatch aggregate
///
/// This trait defines the persistence contract for PickingBatch entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PickingBatchRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new picking_batch entity
    async fn save(&self, entity: &PickingBatch) -> Result<PickingBatch>;

    /// Find picking_batch by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PickingBatch>>;

    /// Find all picking_batch entities
    async fn find_all(&self) -> Result<Vec<PickingBatch>>;

    /// Update picking_batch by ID
    async fn update(&self, id: &str, entity: &PickingBatch) -> Result<Option<PickingBatch>>;

    /// Delete picking_batch by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List picking_batch with pagination
    async fn list(&self, params: PickingBatchPaginationParams) -> Result<PickingBatchPaginatedResult>;

    /// List picking_batch with pagination and filters
    async fn list_with_filters(&self, params: PickingBatchPaginationParams, filters: PickingBatchFilter) -> Result<PickingBatchPaginatedResult>;

    /// Count all picking_batch entities
    async fn count(&self) -> Result<u64>;

    /// Count picking_batch entities matching filters
    async fn count_with_filters(&self, filters: PickingBatchFilter) -> Result<u64>;

    /// Check if picking_batch exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete picking_batch by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted picking_batch by ID
    async fn restore(&self, id: &str) -> Result<Option<PickingBatch>>;

    /// List soft-deleted picking_batch entities
    async fn list_deleted(&self, params: PickingBatchPaginationParams) -> Result<PickingBatchPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save picking_batch entities
    async fn bulk_save(&self, entities: &[PickingBatch]) -> Result<Vec<PickingBatch>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
