//! Repository trait for Scrap aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Scrap aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Scrap, ScrapState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ScrapPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ScrapPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ScrapPaginatedResult {
    pub data: Vec<Scrap>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ScrapFilter {
    pub name: Option<String>,
    pub state: Option<ScrapState>,
    pub origin: Option<String>,
    pub item_id: Option<Uuid>,
    pub location_id: Option<Uuid>,
    pub scrap_location_id: Option<Uuid>,
    pub lot_id: Option<Uuid>,
    pub package_id: Option<Uuid>,
    pub owner_id: Option<Uuid>,
    pub picking_id: Option<Uuid>,
    pub move_id: Option<Uuid>,
}

impl ScrapFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.state.is_some() || self.origin.is_some() || self.item_id.is_some() || self.location_id.is_some() || self.scrap_location_id.is_some() || self.lot_id.is_some() || self.package_id.is_some() || self.owner_id.is_some() || self.picking_id.is_some() || self.move_id.is_some()
    }
}

/// Repository trait for Scrap aggregate
///
/// This trait defines the persistence contract for Scrap entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ScrapRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new scrap entity
    async fn save(&self, entity: &Scrap) -> Result<Scrap>;

    /// Find scrap by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Scrap>>;

    /// Find all scrap entities
    async fn find_all(&self) -> Result<Vec<Scrap>>;

    /// Update scrap by ID
    async fn update(&self, id: &str, entity: &Scrap) -> Result<Option<Scrap>>;

    /// Delete scrap by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List scrap with pagination
    async fn list(&self, params: ScrapPaginationParams) -> Result<ScrapPaginatedResult>;

    /// List scrap with pagination and filters
    async fn list_with_filters(&self, params: ScrapPaginationParams, filters: ScrapFilter) -> Result<ScrapPaginatedResult>;

    /// Count all scrap entities
    async fn count(&self) -> Result<u64>;

    /// Count scrap entities matching filters
    async fn count_with_filters(&self, filters: ScrapFilter) -> Result<u64>;

    /// Check if scrap exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete scrap by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted scrap by ID
    async fn restore(&self, id: &str) -> Result<Option<Scrap>>;

    /// List soft-deleted scrap entities
    async fn list_deleted(&self, params: ScrapPaginationParams) -> Result<ScrapPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save scrap entities
    async fn bulk_save(&self, entities: &[Scrap]) -> Result<Vec<Scrap>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
