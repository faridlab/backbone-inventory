//! Repository trait for LandedCostLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LandedCostLine aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{LandedCostLine, LandedCostSplitMethod};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LandedCostLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LandedCostLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LandedCostLinePaginatedResult {
    pub data: Vec<LandedCostLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LandedCostLineFilter {
    pub lc_id: Option<Uuid>,
    pub name: Option<String>,
    pub account_id: Option<Uuid>,
    pub split_method: Option<LandedCostSplitMethod>,
}

impl LandedCostLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.lc_id.is_some() || self.name.is_some() || self.account_id.is_some() || self.split_method.is_some()
    }
}

/// Repository trait for LandedCostLine aggregate
///
/// This trait defines the persistence contract for LandedCostLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LandedCostLineRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new landed_cost_line entity
    async fn save(&self, entity: &LandedCostLine) -> Result<LandedCostLine>;

    /// Find landed_cost_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LandedCostLine>>;

    /// Find all landed_cost_line entities
    async fn find_all(&self) -> Result<Vec<LandedCostLine>>;

    /// Update landed_cost_line by ID
    async fn update(&self, id: &str, entity: &LandedCostLine) -> Result<Option<LandedCostLine>>;

    /// Delete landed_cost_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List landed_cost_line with pagination
    async fn list(&self, params: LandedCostLinePaginationParams) -> Result<LandedCostLinePaginatedResult>;

    /// List landed_cost_line with pagination and filters
    async fn list_with_filters(&self, params: LandedCostLinePaginationParams, filters: LandedCostLineFilter) -> Result<LandedCostLinePaginatedResult>;

    /// Count all landed_cost_line entities
    async fn count(&self) -> Result<u64>;

    /// Count landed_cost_line entities matching filters
    async fn count_with_filters(&self, filters: LandedCostLineFilter) -> Result<u64>;

    /// Check if landed_cost_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete landed_cost_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted landed_cost_line by ID
    async fn restore(&self, id: &str) -> Result<Option<LandedCostLine>>;

    /// List soft-deleted landed_cost_line entities
    async fn list_deleted(&self, params: LandedCostLinePaginationParams) -> Result<LandedCostLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save landed_cost_line entities
    async fn bulk_save(&self, entities: &[LandedCostLine]) -> Result<Vec<LandedCostLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
