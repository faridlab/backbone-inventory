//! Repository trait for PurchaseReceiptItem aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PurchaseReceiptItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::PurchaseReceiptItem;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PurchaseReceiptItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PurchaseReceiptItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PurchaseReceiptItemPaginatedResult {
    pub data: Vec<PurchaseReceiptItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PurchaseReceiptItemFilter {
    pub receipt_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
}

impl PurchaseReceiptItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.receipt_id.is_some() || self.company_id.is_some() || self.item_id.is_some()
    }
}

/// Repository trait for PurchaseReceiptItem aggregate
///
/// This trait defines the persistence contract for PurchaseReceiptItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PurchaseReceiptItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new purchase_receipt_item entity
    async fn save(&self, entity: &PurchaseReceiptItem) -> Result<PurchaseReceiptItem>;

    /// Find purchase_receipt_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PurchaseReceiptItem>>;

    /// Find all purchase_receipt_item entities
    async fn find_all(&self) -> Result<Vec<PurchaseReceiptItem>>;

    /// Update purchase_receipt_item by ID
    async fn update(&self, id: &str, entity: &PurchaseReceiptItem) -> Result<Option<PurchaseReceiptItem>>;

    /// Delete purchase_receipt_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List purchase_receipt_item with pagination
    async fn list(&self, params: PurchaseReceiptItemPaginationParams) -> Result<PurchaseReceiptItemPaginatedResult>;

    /// List purchase_receipt_item with pagination and filters
    async fn list_with_filters(&self, params: PurchaseReceiptItemPaginationParams, filters: PurchaseReceiptItemFilter) -> Result<PurchaseReceiptItemPaginatedResult>;

    /// Count all purchase_receipt_item entities
    async fn count(&self) -> Result<u64>;

    /// Count purchase_receipt_item entities matching filters
    async fn count_with_filters(&self, filters: PurchaseReceiptItemFilter) -> Result<u64>;

    /// Check if purchase_receipt_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete purchase_receipt_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted purchase_receipt_item by ID
    async fn restore(&self, id: &str) -> Result<Option<PurchaseReceiptItem>>;

    /// List soft-deleted purchase_receipt_item entities
    async fn list_deleted(&self, params: PurchaseReceiptItemPaginationParams) -> Result<PurchaseReceiptItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save purchase_receipt_item entities
    async fn bulk_save(&self, entities: &[PurchaseReceiptItem]) -> Result<Vec<PurchaseReceiptItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
