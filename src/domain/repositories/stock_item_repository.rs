//! Repository trait for StockItem aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockItem, ValuationMethod};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockItemPaginatedResult {
    pub data: Vec<StockItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockItemFilter {
    pub item_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub stock_uom: Option<String>,
    pub is_stock_item: Option<bool>,
    pub has_batch: Option<bool>,
    pub valuation_method: Option<ValuationMethod>,
}

impl StockItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.item_id.is_some() || self.company_id.is_some() || self.stock_uom.is_some() || self.is_stock_item.is_some() || self.has_batch.is_some() || self.valuation_method.is_some()
    }
}

/// Repository trait for StockItem aggregate
///
/// This trait defines the persistence contract for StockItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_item entity
    async fn save(&self, entity: &StockItem) -> Result<StockItem>;

    /// Find stock_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockItem>>;

    /// Find all stock_item entities
    async fn find_all(&self) -> Result<Vec<StockItem>>;

    /// Update stock_item by ID
    async fn update(&self, id: &str, entity: &StockItem) -> Result<Option<StockItem>>;

    /// Delete stock_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_item with pagination
    async fn list(&self, params: StockItemPaginationParams) -> Result<StockItemPaginatedResult>;

    /// List stock_item with pagination and filters
    async fn list_with_filters(&self, params: StockItemPaginationParams, filters: StockItemFilter) -> Result<StockItemPaginatedResult>;

    /// Count all stock_item entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_item entities matching filters
    async fn count_with_filters(&self, filters: StockItemFilter) -> Result<u64>;

    /// Check if stock_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_item by ID
    async fn restore(&self, id: &str) -> Result<Option<StockItem>>;

    /// List soft-deleted stock_item entities
    async fn list_deleted(&self, params: StockItemPaginationParams) -> Result<StockItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_item entities
    async fn bulk_save(&self, entities: &[StockItem]) -> Result<Vec<StockItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
