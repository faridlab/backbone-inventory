//! Repository trait for Route aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Route aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::Route;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RoutePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RoutePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RoutePaginatedResult {
    pub data: Vec<Route>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RouteFilter {
    pub name: Option<String>,
    pub active: Option<bool>,
    pub product_selectable: Option<bool>,
    pub product_categ_selectable: Option<bool>,
    pub warehouse_selectable: Option<bool>,
}

impl RouteFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.active.is_some() || self.product_selectable.is_some() || self.product_categ_selectable.is_some() || self.warehouse_selectable.is_some()
    }
}

/// Repository trait for Route aggregate
///
/// This trait defines the persistence contract for Route entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RouteRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new route entity
    async fn save(&self, entity: &Route) -> Result<Route>;

    /// Find route by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Route>>;

    /// Find all route entities
    async fn find_all(&self) -> Result<Vec<Route>>;

    /// Update route by ID
    async fn update(&self, id: &str, entity: &Route) -> Result<Option<Route>>;

    /// Delete route by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List route with pagination
    async fn list(&self, params: RoutePaginationParams) -> Result<RoutePaginatedResult>;

    /// List route with pagination and filters
    async fn list_with_filters(&self, params: RoutePaginationParams, filters: RouteFilter) -> Result<RoutePaginatedResult>;

    /// Count all route entities
    async fn count(&self) -> Result<u64>;

    /// Count route entities matching filters
    async fn count_with_filters(&self, filters: RouteFilter) -> Result<u64>;

    /// Check if route exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete route by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted route by ID
    async fn restore(&self, id: &str) -> Result<Option<Route>>;

    /// List soft-deleted route entities
    async fn list_deleted(&self, params: RoutePaginationParams) -> Result<RoutePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save route entities
    async fn bulk_save(&self, entities: &[Route]) -> Result<Vec<Route>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
