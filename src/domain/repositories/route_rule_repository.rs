//! Repository trait for RouteRule aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RouteRule aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{RouteRule, ProcureMethod, RuleAction, RuleAuto};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RouteRulePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RouteRulePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RouteRulePaginatedResult {
    pub data: Vec<RouteRule>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RouteRuleFilter {
    pub name: Option<String>,
    pub active: Option<bool>,
    pub action: Option<RuleAction>,
    pub auto: Option<RuleAuto>,
    pub procure_method: Option<ProcureMethod>,
    pub location_src_id: Option<Uuid>,
    pub location_dest_id: Option<Uuid>,
    pub picking_type_id: Option<Uuid>,
    pub route_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub propagate_cancel: Option<bool>,
}

impl RouteRuleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.active.is_some() || self.action.is_some() || self.auto.is_some() || self.procure_method.is_some() || self.location_src_id.is_some() || self.location_dest_id.is_some() || self.picking_type_id.is_some() || self.route_id.is_some() || self.warehouse_id.is_some() || self.company_id.is_some() || self.propagate_cancel.is_some()
    }
}

/// Repository trait for RouteRule aggregate
///
/// This trait defines the persistence contract for RouteRule entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RouteRuleRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new route_rule entity
    async fn save(&self, entity: &RouteRule) -> Result<RouteRule>;

    /// Find route_rule by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RouteRule>>;

    /// Find all route_rule entities
    async fn find_all(&self) -> Result<Vec<RouteRule>>;

    /// Update route_rule by ID
    async fn update(&self, id: &str, entity: &RouteRule) -> Result<Option<RouteRule>>;

    /// Delete route_rule by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List route_rule with pagination
    async fn list(&self, params: RouteRulePaginationParams) -> Result<RouteRulePaginatedResult>;

    /// List route_rule with pagination and filters
    async fn list_with_filters(&self, params: RouteRulePaginationParams, filters: RouteRuleFilter) -> Result<RouteRulePaginatedResult>;

    /// Count all route_rule entities
    async fn count(&self) -> Result<u64>;

    /// Count route_rule entities matching filters
    async fn count_with_filters(&self, filters: RouteRuleFilter) -> Result<u64>;

    /// Check if route_rule exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete route_rule by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted route_rule by ID
    async fn restore(&self, id: &str) -> Result<Option<RouteRule>>;

    /// List soft-deleted route_rule entities
    async fn list_deleted(&self, params: RouteRulePaginationParams) -> Result<RouteRulePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save route_rule entities
    async fn bulk_save(&self, entities: &[RouteRule]) -> Result<Vec<RouteRule>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
