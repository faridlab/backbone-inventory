//! Repository trait for StockTransferItem aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockTransferItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockTransferItem, TransferItemStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockTransferItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockTransferItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockTransferItemPaginatedResult {
    pub data: Vec<StockTransferItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockTransferItemFilter {
    pub transfer_id: Option<Uuid>,
    pub product_id: Option<Uuid>,
    pub uom: Option<String>,
    pub batch_id: Option<Uuid>,
    pub batch_number: Option<String>,
    pub from_bin: Option<String>,
    pub to_bin: Option<String>,
    pub status: Option<TransferItemStatus>,
    pub rejection_reason: Option<String>,
    pub notes: Option<String>,
}

impl StockTransferItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.transfer_id.is_some() || self.product_id.is_some() || self.uom.is_some() || self.batch_id.is_some() || self.batch_number.is_some() || self.from_bin.is_some() || self.to_bin.is_some() || self.status.is_some() || self.rejection_reason.is_some() || self.notes.is_some()
    }
}

/// Repository trait for StockTransferItem aggregate
///
/// This trait defines the persistence contract for StockTransferItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockTransferItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_transfer_item entity
    async fn save(&self, entity: &StockTransferItem) -> Result<StockTransferItem>;

    /// Find stock_transfer_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockTransferItem>>;

    /// Find all stock_transfer_item entities
    async fn find_all(&self) -> Result<Vec<StockTransferItem>>;

    /// Update stock_transfer_item by ID
    async fn update(&self, id: &str, entity: &StockTransferItem) -> Result<Option<StockTransferItem>>;

    /// Delete stock_transfer_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_transfer_item with pagination
    async fn list(&self, params: StockTransferItemPaginationParams) -> Result<StockTransferItemPaginatedResult>;

    /// List stock_transfer_item with pagination and filters
    async fn list_with_filters(&self, params: StockTransferItemPaginationParams, filters: StockTransferItemFilter) -> Result<StockTransferItemPaginatedResult>;

    /// Count all stock_transfer_item entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_transfer_item entities matching filters
    async fn count_with_filters(&self, filters: StockTransferItemFilter) -> Result<u64>;

    /// Check if stock_transfer_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_transfer_item entities
    async fn bulk_save(&self, entities: &[StockTransferItem]) -> Result<Vec<StockTransferItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
