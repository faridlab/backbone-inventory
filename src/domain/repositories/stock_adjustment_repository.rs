//! Repository trait for StockAdjustment aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockAdjustment aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockAdjustment, AdjustmentStatus, AdjustmentType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockAdjustmentPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockAdjustmentPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockAdjustmentPaginatedResult {
    pub data: Vec<StockAdjustment>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockAdjustmentFilter {
    pub provider_id: Option<Uuid>,
    pub adjustment_number: Option<String>,
    pub warehouse_id: Option<Uuid>,
    pub adjustment_type: Option<AdjustmentType>,
    pub is_full_count: Option<bool>,
    pub currency: Option<String>,
    pub status: Option<AdjustmentStatus>,
    pub requires_approval: Option<bool>,
    pub approved_by: Option<Uuid>,
    pub rejection_reason: Option<String>,
    pub posted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notes: Option<String>,
}

impl StockAdjustmentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.adjustment_number.is_some() || self.warehouse_id.is_some() || self.adjustment_type.is_some() || self.is_full_count.is_some() || self.currency.is_some() || self.status.is_some() || self.requires_approval.is_some() || self.approved_by.is_some() || self.rejection_reason.is_some() || self.posted_by.is_some() || self.reason.is_some() || self.notes.is_some()
    }
}

/// Repository trait for StockAdjustment aggregate
///
/// This trait defines the persistence contract for StockAdjustment entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockAdjustmentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_adjustment entity
    async fn save(&self, entity: &StockAdjustment) -> Result<StockAdjustment>;

    /// Find stock_adjustment by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockAdjustment>>;

    /// Find all stock_adjustment entities
    async fn find_all(&self) -> Result<Vec<StockAdjustment>>;

    /// Update stock_adjustment by ID
    async fn update(&self, id: &str, entity: &StockAdjustment) -> Result<Option<StockAdjustment>>;

    /// Delete stock_adjustment by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_adjustment with pagination
    async fn list(&self, params: StockAdjustmentPaginationParams) -> Result<StockAdjustmentPaginatedResult>;

    /// List stock_adjustment with pagination and filters
    async fn list_with_filters(&self, params: StockAdjustmentPaginationParams, filters: StockAdjustmentFilter) -> Result<StockAdjustmentPaginatedResult>;

    /// Count all stock_adjustment entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_adjustment entities matching filters
    async fn count_with_filters(&self, filters: StockAdjustmentFilter) -> Result<u64>;

    /// Check if stock_adjustment exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_adjustment by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_adjustment by ID
    async fn restore(&self, id: &str) -> Result<Option<StockAdjustment>>;

    /// List soft-deleted stock_adjustment entities
    async fn list_deleted(&self, params: StockAdjustmentPaginationParams) -> Result<StockAdjustmentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_adjustment entities
    async fn bulk_save(&self, entities: &[StockAdjustment]) -> Result<Vec<StockAdjustment>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
