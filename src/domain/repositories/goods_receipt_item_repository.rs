//! Repository trait for GoodsReceiptItem aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the GoodsReceiptItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{GoodsReceiptItem, QCResult, ReceiptItemStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct GoodsReceiptItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl GoodsReceiptItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct GoodsReceiptItemPaginatedResult {
    pub data: Vec<GoodsReceiptItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct GoodsReceiptItemFilter {
    pub goods_receipt_id: Option<Uuid>,
    pub product_id: Option<Uuid>,
    pub purchase_order_item_id: Option<Uuid>,
    pub uom: Option<String>,
    pub currency: Option<String>,
    pub batch_number: Option<String>,
    pub warehouse_id: Option<Uuid>,
    pub bin_location: Option<String>,
    pub requires_qc: Option<bool>,
    pub qc_result: Option<QCResult>,
    pub qc_by: Option<Uuid>,
    pub qc_notes: Option<String>,
    pub rejection_reason: Option<String>,
    pub rejection_action: Option<String>,
    pub status: Option<ReceiptItemStatus>,
    pub stock_movement_id: Option<Uuid>,
    pub is_posted: Option<bool>,
    pub notes: Option<String>,
}

impl GoodsReceiptItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.goods_receipt_id.is_some() || self.product_id.is_some() || self.purchase_order_item_id.is_some() || self.uom.is_some() || self.currency.is_some() || self.batch_number.is_some() || self.warehouse_id.is_some() || self.bin_location.is_some() || self.requires_qc.is_some() || self.qc_result.is_some() || self.qc_by.is_some() || self.qc_notes.is_some() || self.rejection_reason.is_some() || self.rejection_action.is_some() || self.status.is_some() || self.stock_movement_id.is_some() || self.is_posted.is_some() || self.notes.is_some()
    }
}

/// Repository trait for GoodsReceiptItem aggregate
///
/// This trait defines the persistence contract for GoodsReceiptItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait GoodsReceiptItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new goods_receipt_item entity
    async fn save(&self, entity: &GoodsReceiptItem) -> Result<GoodsReceiptItem>;

    /// Find goods_receipt_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<GoodsReceiptItem>>;

    /// Find all goods_receipt_item entities
    async fn find_all(&self) -> Result<Vec<GoodsReceiptItem>>;

    /// Update goods_receipt_item by ID
    async fn update(&self, id: &str, entity: &GoodsReceiptItem) -> Result<Option<GoodsReceiptItem>>;

    /// Delete goods_receipt_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List goods_receipt_item with pagination
    async fn list(&self, params: GoodsReceiptItemPaginationParams) -> Result<GoodsReceiptItemPaginatedResult>;

    /// List goods_receipt_item with pagination and filters
    async fn list_with_filters(&self, params: GoodsReceiptItemPaginationParams, filters: GoodsReceiptItemFilter) -> Result<GoodsReceiptItemPaginatedResult>;

    /// Count all goods_receipt_item entities
    async fn count(&self) -> Result<u64>;

    /// Count goods_receipt_item entities matching filters
    async fn count_with_filters(&self, filters: GoodsReceiptItemFilter) -> Result<u64>;

    /// Check if goods_receipt_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete goods_receipt_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted goods_receipt_item by ID
    async fn restore(&self, id: &str) -> Result<Option<GoodsReceiptItem>>;

    /// List soft-deleted goods_receipt_item entities
    async fn list_deleted(&self, params: GoodsReceiptItemPaginationParams) -> Result<GoodsReceiptItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save goods_receipt_item entities
    async fn bulk_save(&self, entities: &[GoodsReceiptItem]) -> Result<Vec<GoodsReceiptItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
