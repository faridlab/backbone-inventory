//! Repository trait for GoodsReceipt aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the GoodsReceipt aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{GoodsReceipt, GoodsReceiptStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct GoodsReceiptPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl GoodsReceiptPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct GoodsReceiptPaginatedResult {
    pub data: Vec<GoodsReceipt>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct GoodsReceiptFilter {
    pub provider_id: Option<Uuid>,
    pub receipt_number: Option<String>,
    pub purchase_order_id: Option<Uuid>,
    pub supplier_name: Option<String>,
    pub delivery_note: Option<String>,
    pub warehouse_id: Option<Uuid>,
    pub currency: Option<String>,
    pub requires_qc: Option<bool>,
    pub qc_by: Option<Uuid>,
    pub qc_notes: Option<String>,
    pub status: Option<GoodsReceiptStatus>,
    pub received_by: Option<Uuid>,
    pub posted_by: Option<Uuid>,
    pub cancelled_by: Option<Uuid>,
    pub cancellation_reason: Option<String>,
    pub notes: Option<String>,
}

impl GoodsReceiptFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.receipt_number.is_some() || self.purchase_order_id.is_some() || self.supplier_name.is_some() || self.delivery_note.is_some() || self.warehouse_id.is_some() || self.currency.is_some() || self.requires_qc.is_some() || self.qc_by.is_some() || self.qc_notes.is_some() || self.status.is_some() || self.received_by.is_some() || self.posted_by.is_some() || self.cancelled_by.is_some() || self.cancellation_reason.is_some() || self.notes.is_some()
    }
}

/// Repository trait for GoodsReceipt aggregate
///
/// This trait defines the persistence contract for GoodsReceipt entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait GoodsReceiptRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new goods_receipt entity
    async fn save(&self, entity: &GoodsReceipt) -> Result<GoodsReceipt>;

    /// Find goods_receipt by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<GoodsReceipt>>;

    /// Find all goods_receipt entities
    async fn find_all(&self) -> Result<Vec<GoodsReceipt>>;

    /// Update goods_receipt by ID
    async fn update(&self, id: &str, entity: &GoodsReceipt) -> Result<Option<GoodsReceipt>>;

    /// Delete goods_receipt by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List goods_receipt with pagination
    async fn list(&self, params: GoodsReceiptPaginationParams) -> Result<GoodsReceiptPaginatedResult>;

    /// List goods_receipt with pagination and filters
    async fn list_with_filters(&self, params: GoodsReceiptPaginationParams, filters: GoodsReceiptFilter) -> Result<GoodsReceiptPaginatedResult>;

    /// Count all goods_receipt entities
    async fn count(&self) -> Result<u64>;

    /// Count goods_receipt entities matching filters
    async fn count_with_filters(&self, filters: GoodsReceiptFilter) -> Result<u64>;

    /// Check if goods_receipt exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete goods_receipt by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted goods_receipt by ID
    async fn restore(&self, id: &str) -> Result<Option<GoodsReceipt>>;

    /// List soft-deleted goods_receipt entities
    async fn list_deleted(&self, params: GoodsReceiptPaginationParams) -> Result<GoodsReceiptPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save goods_receipt entities
    async fn bulk_save(&self, entities: &[GoodsReceipt]) -> Result<Vec<GoodsReceipt>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
