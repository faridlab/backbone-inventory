//! Repository trait for StockMoveLine aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockMoveLine aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockMoveLine, MoveState};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockMoveLinePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockMoveLinePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockMoveLinePaginatedResult {
    pub data: Vec<StockMoveLine>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockMoveLineFilter {
    pub quantity_product_uom_id: Option<Uuid>,
    pub picked: Option<bool>,
    pub lot_id: Option<Uuid>,
    pub package_id: Option<Uuid>,
    pub result_package_id: Option<Uuid>,
    pub owner_id: Option<Uuid>,
    pub state: Option<MoveState>,
    pub move_id: Option<Uuid>,
    pub picking_id: Option<Uuid>,
    pub location_id: Option<Uuid>,
    pub location_dest_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
}

impl StockMoveLineFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.quantity_product_uom_id.is_some() || self.picked.is_some() || self.lot_id.is_some() || self.package_id.is_some() || self.result_package_id.is_some() || self.owner_id.is_some() || self.state.is_some() || self.move_id.is_some() || self.picking_id.is_some() || self.location_id.is_some() || self.location_dest_id.is_some() || self.item_id.is_some()
    }
}

/// Repository trait for StockMoveLine aggregate
///
/// This trait defines the persistence contract for StockMoveLine entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockMoveLineRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_move_line entity
    async fn save(&self, entity: &StockMoveLine) -> Result<StockMoveLine>;

    /// Find stock_move_line by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockMoveLine>>;

    /// Find all stock_move_line entities
    async fn find_all(&self) -> Result<Vec<StockMoveLine>>;

    /// Update stock_move_line by ID
    async fn update(&self, id: &str, entity: &StockMoveLine) -> Result<Option<StockMoveLine>>;

    /// Delete stock_move_line by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_move_line with pagination
    async fn list(&self, params: StockMoveLinePaginationParams) -> Result<StockMoveLinePaginatedResult>;

    /// List stock_move_line with pagination and filters
    async fn list_with_filters(&self, params: StockMoveLinePaginationParams, filters: StockMoveLineFilter) -> Result<StockMoveLinePaginatedResult>;

    /// Count all stock_move_line entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_move_line entities matching filters
    async fn count_with_filters(&self, filters: StockMoveLineFilter) -> Result<u64>;

    /// Check if stock_move_line exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_move_line by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_move_line by ID
    async fn restore(&self, id: &str) -> Result<Option<StockMoveLine>>;

    /// List soft-deleted stock_move_line entities
    async fn list_deleted(&self, params: StockMoveLinePaginationParams) -> Result<StockMoveLinePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_move_line entities
    async fn bulk_save(&self, entities: &[StockMoveLine]) -> Result<Vec<StockMoveLine>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
