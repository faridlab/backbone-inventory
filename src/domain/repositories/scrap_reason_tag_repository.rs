//! Repository trait for ScrapReasonTag aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ScrapReasonTag aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::ScrapReasonTag;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ScrapReasonTagPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ScrapReasonTagPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ScrapReasonTagPaginatedResult {
    pub data: Vec<ScrapReasonTag>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ScrapReasonTagFilter {
    pub name: Option<String>,
    pub active: Option<bool>,
    pub company_id: Option<Uuid>,
}

impl ScrapReasonTagFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.active.is_some() || self.company_id.is_some()
    }
}

/// Repository trait for ScrapReasonTag aggregate
///
/// This trait defines the persistence contract for ScrapReasonTag entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ScrapReasonTagRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new scrap_reason_tag entity
    async fn save(&self, entity: &ScrapReasonTag) -> Result<ScrapReasonTag>;

    /// Find scrap_reason_tag by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ScrapReasonTag>>;

    /// Find all scrap_reason_tag entities
    async fn find_all(&self) -> Result<Vec<ScrapReasonTag>>;

    /// Update scrap_reason_tag by ID
    async fn update(&self, id: &str, entity: &ScrapReasonTag) -> Result<Option<ScrapReasonTag>>;

    /// Delete scrap_reason_tag by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List scrap_reason_tag with pagination
    async fn list(&self, params: ScrapReasonTagPaginationParams) -> Result<ScrapReasonTagPaginatedResult>;

    /// List scrap_reason_tag with pagination and filters
    async fn list_with_filters(&self, params: ScrapReasonTagPaginationParams, filters: ScrapReasonTagFilter) -> Result<ScrapReasonTagPaginatedResult>;

    /// Count all scrap_reason_tag entities
    async fn count(&self) -> Result<u64>;

    /// Count scrap_reason_tag entities matching filters
    async fn count_with_filters(&self, filters: ScrapReasonTagFilter) -> Result<u64>;

    /// Check if scrap_reason_tag exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete scrap_reason_tag by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted scrap_reason_tag by ID
    async fn restore(&self, id: &str) -> Result<Option<ScrapReasonTag>>;

    /// List soft-deleted scrap_reason_tag entities
    async fn list_deleted(&self, params: ScrapReasonTagPaginationParams) -> Result<ScrapReasonTagPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save scrap_reason_tag entities
    async fn bulk_save(&self, entities: &[ScrapReasonTag]) -> Result<Vec<ScrapReasonTag>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
