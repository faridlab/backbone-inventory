//! Repository trait for StockLedgerEntry aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockLedgerEntry aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockLedgerEntry, VoucherType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockLedgerEntryPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockLedgerEntryPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockLedgerEntryPaginatedResult {
    pub data: Vec<StockLedgerEntry>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockLedgerEntryFilter {
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub voucher_type: Option<VoucherType>,
    pub voucher_id: Option<Uuid>,
    pub voucher_no: Option<String>,
    pub is_cancelled: Option<bool>,
}

impl StockLedgerEntryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.item_id.is_some() || self.warehouse_id.is_some() || self.voucher_type.is_some() || self.voucher_id.is_some() || self.voucher_no.is_some() || self.is_cancelled.is_some()
    }
}

/// Repository trait for StockLedgerEntry aggregate
///
/// This trait defines the persistence contract for StockLedgerEntry entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockLedgerEntryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_ledger_entry entity
    async fn save(&self, entity: &StockLedgerEntry) -> Result<StockLedgerEntry>;

    /// Find stock_ledger_entry by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockLedgerEntry>>;

    /// Find all stock_ledger_entry entities
    async fn find_all(&self) -> Result<Vec<StockLedgerEntry>>;

    /// Update stock_ledger_entry by ID
    async fn update(&self, id: &str, entity: &StockLedgerEntry) -> Result<Option<StockLedgerEntry>>;

    /// Delete stock_ledger_entry by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_ledger_entry with pagination
    async fn list(&self, params: StockLedgerEntryPaginationParams) -> Result<StockLedgerEntryPaginatedResult>;

    /// List stock_ledger_entry with pagination and filters
    async fn list_with_filters(&self, params: StockLedgerEntryPaginationParams, filters: StockLedgerEntryFilter) -> Result<StockLedgerEntryPaginatedResult>;

    /// Count all stock_ledger_entry entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_ledger_entry entities matching filters
    async fn count_with_filters(&self, filters: StockLedgerEntryFilter) -> Result<u64>;

    /// Check if stock_ledger_entry exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_ledger_entry by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_ledger_entry by ID
    async fn restore(&self, id: &str) -> Result<Option<StockLedgerEntry>>;

    /// List soft-deleted stock_ledger_entry entities
    async fn list_deleted(&self, params: StockLedgerEntryPaginationParams) -> Result<StockLedgerEntryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_ledger_entry entities
    async fn bulk_save(&self, entities: &[StockLedgerEntry]) -> Result<Vec<StockLedgerEntry>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
