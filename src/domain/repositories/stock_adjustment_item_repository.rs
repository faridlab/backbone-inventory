//! Repository trait for StockAdjustmentItem aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the StockAdjustmentItem aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{StockAdjustmentItem, UnitOfMeasure};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockAdjustmentItemPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StockAdjustmentItemPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StockAdjustmentItemPaginatedResult {
    pub data: Vec<StockAdjustmentItem>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StockAdjustmentItemFilter {
    pub adjustment_id: Option<Uuid>,
    pub provider_id: Option<Uuid>,
    pub product_id: Option<Uuid>,
    pub stock_id: Option<Uuid>,
    pub batch_id: Option<Uuid>,
    pub batch_number: Option<String>,
    pub uom: Option<UnitOfMeasure>,
    pub currency: Option<String>,
    pub variance_reason: Option<String>,
    pub bin_location: Option<String>,
    pub is_posted: Option<bool>,
    pub movement_id: Option<Uuid>,
    pub notes: Option<String>,
}

impl StockAdjustmentItemFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.adjustment_id.is_some() || self.provider_id.is_some() || self.product_id.is_some() || self.stock_id.is_some() || self.batch_id.is_some() || self.batch_number.is_some() || self.uom.is_some() || self.currency.is_some() || self.variance_reason.is_some() || self.bin_location.is_some() || self.is_posted.is_some() || self.movement_id.is_some() || self.notes.is_some()
    }
}

/// Repository trait for StockAdjustmentItem aggregate
///
/// This trait defines the persistence contract for StockAdjustmentItem entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StockAdjustmentItemRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stock_adjustment_item entity
    async fn save(&self, entity: &StockAdjustmentItem) -> Result<StockAdjustmentItem>;

    /// Find stock_adjustment_item by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<StockAdjustmentItem>>;

    /// Find all stock_adjustment_item entities
    async fn find_all(&self) -> Result<Vec<StockAdjustmentItem>>;

    /// Update stock_adjustment_item by ID
    async fn update(&self, id: &str, entity: &StockAdjustmentItem) -> Result<Option<StockAdjustmentItem>>;

    /// Delete stock_adjustment_item by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stock_adjustment_item with pagination
    async fn list(&self, params: StockAdjustmentItemPaginationParams) -> Result<StockAdjustmentItemPaginatedResult>;

    /// List stock_adjustment_item with pagination and filters
    async fn list_with_filters(&self, params: StockAdjustmentItemPaginationParams, filters: StockAdjustmentItemFilter) -> Result<StockAdjustmentItemPaginatedResult>;

    /// Count all stock_adjustment_item entities
    async fn count(&self) -> Result<u64>;

    /// Count stock_adjustment_item entities matching filters
    async fn count_with_filters(&self, filters: StockAdjustmentItemFilter) -> Result<u64>;

    /// Check if stock_adjustment_item exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stock_adjustment_item by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stock_adjustment_item by ID
    async fn restore(&self, id: &str) -> Result<Option<StockAdjustmentItem>>;

    /// List soft-deleted stock_adjustment_item entities
    async fn list_deleted(&self, params: StockAdjustmentItemPaginationParams) -> Result<StockAdjustmentItemPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stock_adjustment_item entities
    async fn bulk_save(&self, entities: &[StockAdjustmentItem]) -> Result<Vec<StockAdjustmentItem>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
