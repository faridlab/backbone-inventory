//! Repository trait for Product aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Product aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Product, CostingMethod, ProductStatus, ProductType, UnitOfMeasure};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProductPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ProductPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ProductPaginatedResult {
    pub data: Vec<Product>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProductFilter {
    pub provider_id: Option<Uuid>,
    pub sku: Option<String>,
    pub barcode: Option<String>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub product_type: Option<ProductType>,
    pub category_id: Option<Uuid>,
    pub brand: Option<String>,
    pub manufacturer: Option<String>,
    pub base_uom: Option<UnitOfMeasure>,
    pub purchase_uom: Option<UnitOfMeasure>,
    pub secondary_uom: Option<UnitOfMeasure>,
    pub costing_method: Option<CostingMethod>,
    pub currency: Option<String>,
    pub is_stockable: Option<bool>,
    pub track_batches: Option<bool>,
    pub track_expiry: Option<bool>,
    pub storage_conditions: Option<String>,
    pub status: Option<ProductStatus>,
    pub notes: Option<String>,
}

impl ProductFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_id.is_some() || self.sku.is_some() || self.barcode.is_some() || self.name.is_some() || self.description.is_some() || self.product_type.is_some() || self.category_id.is_some() || self.brand.is_some() || self.manufacturer.is_some() || self.base_uom.is_some() || self.purchase_uom.is_some() || self.secondary_uom.is_some() || self.costing_method.is_some() || self.currency.is_some() || self.is_stockable.is_some() || self.track_batches.is_some() || self.track_expiry.is_some() || self.storage_conditions.is_some() || self.status.is_some() || self.notes.is_some()
    }
}

/// Repository trait for Product aggregate
///
/// This trait defines the persistence contract for Product entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ProductRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new product entity
    async fn save(&self, entity: &Product) -> Result<Product>;

    /// Find product by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Product>>;

    /// Find all product entities
    async fn find_all(&self) -> Result<Vec<Product>>;

    /// Update product by ID
    async fn update(&self, id: &str, entity: &Product) -> Result<Option<Product>>;

    /// Delete product by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List product with pagination
    async fn list(&self, params: ProductPaginationParams) -> Result<ProductPaginatedResult>;

    /// List product with pagination and filters
    async fn list_with_filters(&self, params: ProductPaginationParams, filters: ProductFilter) -> Result<ProductPaginatedResult>;

    /// Count all product entities
    async fn count(&self) -> Result<u64>;

    /// Count product entities matching filters
    async fn count_with_filters(&self, filters: ProductFilter) -> Result<u64>;

    /// Check if product exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete product by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted product by ID
    async fn restore(&self, id: &str) -> Result<Option<Product>>;

    /// List soft-deleted product entities
    async fn list_deleted(&self, params: ProductPaginationParams) -> Result<ProductPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save product entities
    async fn bulk_save(&self, entities: &[Product]) -> Result<Vec<Product>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
