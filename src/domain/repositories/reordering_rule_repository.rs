//! Repository trait for ReorderingRule aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ReorderingRule aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ReorderingRule, OrderpointTrigger};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ReorderingRulePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ReorderingRulePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ReorderingRulePaginatedResult {
    pub data: Vec<ReorderingRule>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ReorderingRuleFilter {
    pub name: Option<String>,
    pub trigger: Option<OrderpointTrigger>,
    pub active: Option<bool>,
    pub item_id: Option<Uuid>,
    pub location_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
    pub route_id: Option<Uuid>,
}

impl ReorderingRuleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.trigger.is_some() || self.active.is_some() || self.item_id.is_some() || self.location_id.is_some() || self.warehouse_id.is_some() || self.company_id.is_some() || self.route_id.is_some()
    }
}

/// Repository trait for ReorderingRule aggregate
///
/// This trait defines the persistence contract for ReorderingRule entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ReorderingRuleRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new reordering_rule entity
    async fn save(&self, entity: &ReorderingRule) -> Result<ReorderingRule>;

    /// Find reordering_rule by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ReorderingRule>>;

    /// Find all reordering_rule entities
    async fn find_all(&self) -> Result<Vec<ReorderingRule>>;

    /// Update reordering_rule by ID
    async fn update(&self, id: &str, entity: &ReorderingRule) -> Result<Option<ReorderingRule>>;

    /// Delete reordering_rule by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List reordering_rule with pagination
    async fn list(&self, params: ReorderingRulePaginationParams) -> Result<ReorderingRulePaginatedResult>;

    /// List reordering_rule with pagination and filters
    async fn list_with_filters(&self, params: ReorderingRulePaginationParams, filters: ReorderingRuleFilter) -> Result<ReorderingRulePaginatedResult>;

    /// Count all reordering_rule entities
    async fn count(&self) -> Result<u64>;

    /// Count reordering_rule entities matching filters
    async fn count_with_filters(&self, filters: ReorderingRuleFilter) -> Result<u64>;

    /// Check if reordering_rule exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete reordering_rule by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted reordering_rule by ID
    async fn restore(&self, id: &str) -> Result<Option<ReorderingRule>>;

    /// List soft-deleted reordering_rule entities
    async fn list_deleted(&self, params: ReorderingRulePaginationParams) -> Result<ReorderingRulePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save reordering_rule entities
    async fn bulk_save(&self, entities: &[ReorderingRule]) -> Result<Vec<ReorderingRule>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
