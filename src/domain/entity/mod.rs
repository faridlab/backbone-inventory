//! Domain Entities
//!
//! Generated by backbone-schema. Do not edit manually.

pub mod goods_receipt;
pub mod goods_receipt_item;
pub mod product_category;
pub mod product;
pub mod purchase_order;
pub mod purchase_order_item;
pub mod stock;
pub mod stock_adjustment;
pub mod stock_adjustment_item;
pub mod stock_batch;
pub mod stock_movement;
pub mod stock_transfer;
pub mod stock_transfer_item;
pub mod warehouse;
pub mod goods_receipt_status;
pub mod receipt_item_status;
pub mod qc_result;
pub mod product_type;
pub mod unit_of_measure;
pub mod costing_method;
pub mod product_status;
pub mod purchase_order_type;
pub mod purchase_order_status;
pub mod quality_status;
pub mod adjustment_type;
pub mod adjustment_status;
pub mod batch_status;
pub mod movement_type;
pub mod movement_reason;
pub mod movement_status;
pub mod transfer_type;
pub mod transfer_status;
pub mod transfer_item_status;
pub mod warehouse_type;
pub mod warehouse_status;

// Re-exports
pub use goods_receipt::GoodsReceipt;
pub use goods_receipt::GoodsReceiptBuilder;
pub use goods_receipt::GoodsReceiptId;
pub use goods_receipt_item::GoodsReceiptItem;
pub use goods_receipt_item::GoodsReceiptItemBuilder;
pub use goods_receipt_item::GoodsReceiptItemId;
pub use product_category::ProductCategory;
pub use product_category::ProductCategoryBuilder;
pub use product_category::ProductCategoryId;
pub use product::Product;
pub use product::ProductBuilder;
pub use product::ProductId;
pub use purchase_order::PurchaseOrder;
pub use purchase_order::PurchaseOrderBuilder;
pub use purchase_order::PurchaseOrderId;
pub use purchase_order_item::PurchaseOrderItem;
pub use purchase_order_item::PurchaseOrderItemBuilder;
pub use purchase_order_item::PurchaseOrderItemId;
pub use stock::Stock;
pub use stock::StockBuilder;
pub use stock::StockId;
pub use stock_adjustment::StockAdjustment;
pub use stock_adjustment::StockAdjustmentBuilder;
pub use stock_adjustment::StockAdjustmentId;
pub use stock_adjustment_item::StockAdjustmentItem;
pub use stock_adjustment_item::StockAdjustmentItemBuilder;
pub use stock_adjustment_item::StockAdjustmentItemId;
pub use stock_batch::StockBatch;
pub use stock_batch::StockBatchBuilder;
pub use stock_batch::StockBatchId;
pub use stock_movement::StockMovement;
pub use stock_movement::StockMovementBuilder;
pub use stock_movement::StockMovementId;
pub use stock_transfer::StockTransfer;
pub use stock_transfer::StockTransferBuilder;
pub use stock_transfer::StockTransferId;
pub use stock_transfer_item::StockTransferItem;
pub use stock_transfer_item::StockTransferItemBuilder;
pub use stock_transfer_item::StockTransferItemId;
pub use warehouse::Warehouse;
pub use warehouse::WarehouseBuilder;
pub use warehouse::WarehouseId;
pub use goods_receipt_status::GoodsReceiptStatus;
pub use receipt_item_status::ReceiptItemStatus;
pub use qc_result::QCResult;
pub use product_type::ProductType;
pub use unit_of_measure::UnitOfMeasure;
pub use costing_method::CostingMethod;
pub use product_status::ProductStatus;
pub use purchase_order_type::PurchaseOrderType;
pub use purchase_order_status::PurchaseOrderStatus;
pub use quality_status::QualityStatus;
pub use adjustment_type::AdjustmentType;
pub use adjustment_status::AdjustmentStatus;
pub use batch_status::BatchStatus;
pub use movement_type::MovementType;
pub use movement_reason::MovementReason;
pub use movement_status::MovementStatus;
pub use transfer_type::TransferType;
pub use transfer_status::TransferStatus;
pub use transfer_item_status::TransferItemStatus;
pub use warehouse_type::WarehouseType;
pub use warehouse_status::WarehouseStatus;

// ==========================================================================
// Entity Trait
// ==========================================================================

use std::fmt::Debug;

/// Trait for domain entities
///
/// All generated entities implement this trait, providing
/// a common interface for working with domain objects.
pub trait Entity: Debug + Clone {
    /// The type of the entity's unique identifier
    type Id: Debug + Clone + PartialEq;

    /// Get the entity's unique identifier
    fn entity_id(&self) -> &Self::Id;

    /// Get the entity type name
    fn entity_type() -> &'static str;
}

// ==========================================================================
// Audit Metadata
// ==========================================================================

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use uuid::Uuid;

/// Audit metadata stored as JSONB in the database
///
/// This struct consolidates audit fields (timestamps and actors) into a single
/// JSONB column for efficient storage and flexible querying.
#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq)]
pub struct AuditMetadata {
    /// Timestamp when the record was created
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was last updated
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was soft-deleted
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_at: Option<DateTime<Utc>>,

    /// User ID who created the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_by: Option<Uuid>,

    /// User ID who last updated the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<Uuid>,

    /// User ID who deleted the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_by: Option<Uuid>,
}

impl AuditMetadata {
    /// Create new audit metadata with created_at set to now
    pub fn new() -> Self {
        Self {
            created_at: Some(Utc::now()),
            ..Default::default()
        }
    }

    /// Create with creator ID
    pub fn with_creator(creator_id: Uuid) -> Self {
        Self {
            created_at: Some(Utc::now()),
            created_by: Some(creator_id),
            ..Default::default()
        }
    }

    /// Touch the record (update updated_at)
    pub fn touch(&mut self) {
        self.updated_at = Some(Utc::now());
    }

    /// Touch with updater ID
    pub fn touch_by(&mut self, updater_id: Uuid) {
        self.updated_at = Some(Utc::now());
        self.updated_by = Some(updater_id);
    }

    /// Soft delete the record
    pub fn soft_delete(&mut self) {
        self.deleted_at = Some(Utc::now());
    }

    /// Soft delete with deleter ID
    pub fn soft_delete_by(&mut self, deleter_id: Uuid) {
        self.deleted_at = Some(Utc::now());
        self.deleted_by = Some(deleter_id);
    }

    /// Restore a soft-deleted record
    pub fn restore(&mut self) {
        self.deleted_at = None;
        self.deleted_by = None;
    }

    /// Check if record is deleted
    pub fn is_deleted(&self) -> bool {
        self.deleted_at.is_some()
    }

    /// Check if record is active (not deleted)
    pub fn is_active(&self) -> bool {
        self.deleted_at.is_none()
    }
}
