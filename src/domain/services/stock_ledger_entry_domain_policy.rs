//! Domain policy for StockLedgerEntry aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use backbone_core::PermitAllPolicy;
use crate::domain::entity::StockLedgerEntry;

/// Domain policy for StockLedgerEntry — permits all operations (no business invariants).
///
/// To add domain rules, replace this alias with a struct implementing
/// `backbone_core::DomainPolicy<StockLedgerEntry>` in the `// <<< CUSTOM` zone.
pub type StockLedgerEntryDomainPolicy = PermitAllPolicy<StockLedgerEntry>;

// <<< CUSTOM
// END CUSTOM
