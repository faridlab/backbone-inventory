//! Application service for LandedCostAdjustmentLine entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `landed_cost_adjustment_line_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::presentation::dto::{CreateLandedCostAdjustmentLineDto, UpdateLandedCostAdjustmentLineDto};
use crate::domain::entity::LandedCostAdjustmentLine;
use crate::infrastructure::persistence::LandedCostAdjustmentLineRepository;

/// Application service for LandedCostAdjustmentLine entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `landed_cost_adjustment_line_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct LandedCostAdjustmentLineServiceCustom {
///     inner: Arc<LandedCostAdjustmentLineService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type LandedCostAdjustmentLineService = GenericCrudService<
    LandedCostAdjustmentLine,
    CreateLandedCostAdjustmentLineDto,
    UpdateLandedCostAdjustmentLineDto,
    LandedCostAdjustmentLineRepository,
>;

// <<< CUSTOM
// END CUSTOM
