//! Validator for LandedCostAdjustmentLine entity
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<LandedCostAdjustmentLine>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use crate::domain::entity::LandedCostAdjustmentLine;

/// Validator type alias for LandedCostAdjustmentLine entities.
pub type LandedCostAdjustmentLineValidator = EntityValidator<LandedCostAdjustmentLine>;

/// Build a validator for LandedCostAdjustmentLine with all schema-defined field rules.
pub fn landed_cost_adjustment_line_validator() -> LandedCostAdjustmentLineValidator {
    EntityValidator::new()
    // <<< CUSTOM RULES
    // END CUSTOM RULES
        // No schema-derived rules — add custom rules above.
}

// <<< CUSTOM
// END CUSTOM
