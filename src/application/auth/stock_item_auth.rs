//! Auth configuration for StockItem handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockItemService;
use crate::domain::entity::StockItem;

/// Permission string constants for StockItem operations.
pub mod permissions {
    pub const LIST:        &str = "stock_item:list";
    pub const READ:        &str = "stock_item:read";
    pub const CREATE:      &str = "stock_item:create";
    pub const UPDATE:      &str = "stock_item:update";
    pub const DELETE:      &str = "stock_item:delete";
    pub const RESTORE:     &str = "stock_item:restore";
    pub const TRASH:       &str = "stock_item:trash";
    pub const EMPTY_TRASH: &str = "stock_item:empty_trash";
    pub const BULK_CREATE: &str = "stock_item:bulk_create";
    pub const UPSERT:      &str = "stock_item:upsert";
}

/// Resource policy for StockItem — maps CRUD actions to permission strings.
pub struct StockItemPolicy;

#[async_trait]
impl ResourcePolicy<StockItem> for StockItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockItem,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for StockItem handlers.
///
/// Wraps `StockItemService` and checks `StockItemPolicy` before delegating.
pub type StockItemGuard = PermissionGuard<StockItem>;

/// Service-integrated permission guard for StockItem (for use in generated handlers).
pub type StockItemServiceGuard = ServicePermissionGuard<StockItem, StockItemService, StockItemPolicy>;

// <<< CUSTOM
// END CUSTOM
