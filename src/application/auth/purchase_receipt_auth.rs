//! Auth configuration for PurchaseReceipt handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PurchaseReceipt>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PurchaseReceiptService;
use crate::domain::entity::PurchaseReceipt;

/// Permission string constants for PurchaseReceipt operations.
pub mod permissions {
    pub const LIST:        &str = "purchase_receipt:list";
    pub const READ:        &str = "purchase_receipt:read";
    pub const CREATE:      &str = "purchase_receipt:create";
    pub const UPDATE:      &str = "purchase_receipt:update";
    pub const DELETE:      &str = "purchase_receipt:delete";
    pub const RESTORE:     &str = "purchase_receipt:restore";
    pub const TRASH:       &str = "purchase_receipt:trash";
    pub const EMPTY_TRASH: &str = "purchase_receipt:empty_trash";
    pub const BULK_CREATE: &str = "purchase_receipt:bulk_create";
    pub const UPSERT:      &str = "purchase_receipt:upsert";
}

/// Resource policy for PurchaseReceipt — maps CRUD actions to permission strings.
pub struct PurchaseReceiptPolicy;

#[async_trait]
impl ResourcePolicy<PurchaseReceipt> for PurchaseReceiptPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PurchaseReceipt,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PurchaseReceipt handlers.
///
/// Wraps `PurchaseReceiptService` and checks `PurchaseReceiptPolicy` before delegating.
pub type PurchaseReceiptGuard = PermissionGuard<PurchaseReceipt>;

/// Service-integrated permission guard for PurchaseReceipt (for use in generated handlers).
pub type PurchaseReceiptServiceGuard = ServicePermissionGuard<PurchaseReceipt, PurchaseReceiptService, PurchaseReceiptPolicy>;

// <<< CUSTOM
// END CUSTOM
