//! Auth configuration for DeliveryNote handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DeliveryNote>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DeliveryNoteService;
use crate::domain::entity::DeliveryNote;

/// Permission string constants for DeliveryNote operations.
pub mod permissions {
    pub const LIST:        &str = "delivery_note:list";
    pub const READ:        &str = "delivery_note:read";
    pub const CREATE:      &str = "delivery_note:create";
    pub const UPDATE:      &str = "delivery_note:update";
    pub const DELETE:      &str = "delivery_note:delete";
    pub const RESTORE:     &str = "delivery_note:restore";
    pub const TRASH:       &str = "delivery_note:trash";
    pub const EMPTY_TRASH: &str = "delivery_note:empty_trash";
    pub const BULK_CREATE: &str = "delivery_note:bulk_create";
    pub const UPSERT:      &str = "delivery_note:upsert";
}

/// Resource policy for DeliveryNote — maps CRUD actions to permission strings.
pub struct DeliveryNotePolicy;

#[async_trait]
impl ResourcePolicy<DeliveryNote> for DeliveryNotePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DeliveryNote,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for DeliveryNote handlers.
///
/// Wraps `DeliveryNoteService` and checks `DeliveryNotePolicy` before delegating.
pub type DeliveryNoteGuard = PermissionGuard<DeliveryNote>;

/// Service-integrated permission guard for DeliveryNote (for use in generated handlers).
pub type DeliveryNoteServiceGuard = ServicePermissionGuard<DeliveryNote, DeliveryNoteService, DeliveryNotePolicy>;

// <<< CUSTOM
// END CUSTOM
