//! Auth configuration for PurchaseOrderItem handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PurchaseOrderItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PurchaseOrderItemService;
use crate::domain::entity::PurchaseOrderItem;

/// Permission string constants for PurchaseOrderItem operations.
pub mod permissions {
    pub const LIST:        &str = "purchase_order_item:list";
    pub const READ:        &str = "purchase_order_item:read";
    pub const CREATE:      &str = "purchase_order_item:create";
    pub const UPDATE:      &str = "purchase_order_item:update";
    pub const DELETE:      &str = "purchase_order_item:delete";
    pub const RESTORE:     &str = "purchase_order_item:restore";
    pub const TRASH:       &str = "purchase_order_item:trash";
    pub const EMPTY_TRASH: &str = "purchase_order_item:empty_trash";
    pub const BULK_CREATE: &str = "purchase_order_item:bulk_create";
    pub const UPSERT:      &str = "purchase_order_item:upsert";
}

/// Resource policy for PurchaseOrderItem — maps CRUD actions to permission strings.
pub struct PurchaseOrderItemPolicy;

#[async_trait]
impl ResourcePolicy<PurchaseOrderItem> for PurchaseOrderItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PurchaseOrderItem,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PurchaseOrderItem handlers.
///
/// Wraps `PurchaseOrderItemService` and checks `PurchaseOrderItemPolicy` before delegating.
pub type PurchaseOrderItemGuard = PermissionGuard<PurchaseOrderItem, PurchaseOrderItemService, PurchaseOrderItemPolicy>;

/// Service-integrated permission guard for PurchaseOrderItem (for use in generated handlers).
pub type PurchaseOrderItemServiceGuard = ServicePermissionGuard<PurchaseOrderItem, PurchaseOrderItemService, PurchaseOrderItemPolicy>;

// <<< CUSTOM
// END CUSTOM
