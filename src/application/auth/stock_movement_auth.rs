//! Auth configuration for StockMovement handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockMovement>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockMovementService;
use crate::domain::entity::StockMovement;

/// Permission string constants for StockMovement operations.
pub mod permissions {
    pub const LIST:        &str = "stock_movement:list";
    pub const READ:        &str = "stock_movement:read";
    pub const CREATE:      &str = "stock_movement:create";
    pub const UPDATE:      &str = "stock_movement:update";
    pub const DELETE:      &str = "stock_movement:delete";
    pub const RESTORE:     &str = "stock_movement:restore";
    pub const TRASH:       &str = "stock_movement:trash";
    pub const EMPTY_TRASH: &str = "stock_movement:empty_trash";
    pub const BULK_CREATE: &str = "stock_movement:bulk_create";
    pub const UPSERT:      &str = "stock_movement:upsert";
}

/// Resource policy for StockMovement — maps CRUD actions to permission strings.
pub struct StockMovementPolicy;

#[async_trait]
impl ResourcePolicy<StockMovement> for StockMovementPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockMovement,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for StockMovement handlers.
///
/// Wraps `StockMovementService` and checks `StockMovementPolicy` before delegating.
pub type StockMovementGuard = PermissionGuard<StockMovement, StockMovementService, StockMovementPolicy>;

/// Service-integrated permission guard for StockMovement (for use in generated handlers).
pub type StockMovementServiceGuard = ServicePermissionGuard<StockMovement, StockMovementService, StockMovementPolicy>;

// <<< CUSTOM
// END CUSTOM
