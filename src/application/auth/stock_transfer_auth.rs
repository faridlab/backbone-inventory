//! Auth configuration for StockTransfer handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockTransfer>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockTransferService;
use crate::domain::entity::StockTransfer;

/// Permission string constants for StockTransfer operations.
pub mod permissions {
    pub const LIST:        &str = "stock_transfer:list";
    pub const READ:        &str = "stock_transfer:read";
    pub const CREATE:      &str = "stock_transfer:create";
    pub const UPDATE:      &str = "stock_transfer:update";
    pub const DELETE:      &str = "stock_transfer:delete";
    pub const RESTORE:     &str = "stock_transfer:restore";
    pub const TRASH:       &str = "stock_transfer:trash";
    pub const EMPTY_TRASH: &str = "stock_transfer:empty_trash";
    pub const BULK_CREATE: &str = "stock_transfer:bulk_create";
    pub const UPSERT:      &str = "stock_transfer:upsert";
}

/// Resource policy for StockTransfer — maps CRUD actions to permission strings.
pub struct StockTransferPolicy;

#[async_trait]
impl ResourcePolicy<StockTransfer> for StockTransferPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockTransfer,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for StockTransfer handlers.
///
/// Wraps `StockTransferService` and checks `StockTransferPolicy` before delegating.
pub type StockTransferGuard = PermissionGuard<StockTransfer, StockTransferService, StockTransferPolicy>;

/// Service-integrated permission guard for StockTransfer (for use in generated handlers).
pub type StockTransferServiceGuard = ServicePermissionGuard<StockTransfer, StockTransferService, StockTransferPolicy>;

// <<< CUSTOM
// END CUSTOM
