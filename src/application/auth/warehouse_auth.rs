//! Auth configuration for Warehouse handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Warehouse>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WarehouseService;
use crate::domain::entity::Warehouse;

/// Permission string constants for Warehouse operations.
pub mod permissions {
    pub const LIST:        &str = "warehouse:list";
    pub const READ:        &str = "warehouse:read";
    pub const CREATE:      &str = "warehouse:create";
    pub const UPDATE:      &str = "warehouse:update";
    pub const DELETE:      &str = "warehouse:delete";
    pub const RESTORE:     &str = "warehouse:restore";
    pub const TRASH:       &str = "warehouse:trash";
    pub const EMPTY_TRASH: &str = "warehouse:empty_trash";
    pub const BULK_CREATE: &str = "warehouse:bulk_create";
    pub const UPSERT:      &str = "warehouse:upsert";
}

/// Resource policy for Warehouse — maps CRUD actions to permission strings.
pub struct WarehousePolicy;

#[async_trait]
impl ResourcePolicy<Warehouse> for WarehousePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Warehouse,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Warehouse handlers.
///
/// Wraps `WarehouseService` and checks `WarehousePolicy` before delegating.
pub type WarehouseGuard = PermissionGuard<Warehouse, WarehouseService, WarehousePolicy>;

/// Service-integrated permission guard for Warehouse (for use in generated handlers).
pub type WarehouseServiceGuard = ServicePermissionGuard<Warehouse, WarehouseService, WarehousePolicy>;

// <<< CUSTOM
// END CUSTOM
