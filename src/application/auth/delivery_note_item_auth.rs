//! Auth configuration for DeliveryNoteItem handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DeliveryNoteItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DeliveryNoteItemService;
use crate::domain::entity::DeliveryNoteItem;

/// Permission string constants for DeliveryNoteItem operations.
pub mod permissions {
    pub const LIST:        &str = "delivery_note_item:list";
    pub const READ:        &str = "delivery_note_item:read";
    pub const CREATE:      &str = "delivery_note_item:create";
    pub const UPDATE:      &str = "delivery_note_item:update";
    pub const DELETE:      &str = "delivery_note_item:delete";
    pub const RESTORE:     &str = "delivery_note_item:restore";
    pub const TRASH:       &str = "delivery_note_item:trash";
    pub const EMPTY_TRASH: &str = "delivery_note_item:empty_trash";
    pub const BULK_CREATE: &str = "delivery_note_item:bulk_create";
    pub const UPSERT:      &str = "delivery_note_item:upsert";
}

/// Resource policy for DeliveryNoteItem — maps CRUD actions to permission strings.
pub struct DeliveryNoteItemPolicy;

#[async_trait]
impl ResourcePolicy<DeliveryNoteItem> for DeliveryNoteItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DeliveryNoteItem,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for DeliveryNoteItem handlers.
///
/// Wraps `DeliveryNoteItemService` and checks `DeliveryNoteItemPolicy` before delegating.
pub type DeliveryNoteItemGuard = PermissionGuard<DeliveryNoteItem>;

/// Service-integrated permission guard for DeliveryNoteItem (for use in generated handlers).
pub type DeliveryNoteItemServiceGuard = ServicePermissionGuard<DeliveryNoteItem, DeliveryNoteItemService, DeliveryNoteItemPolicy>;

// <<< CUSTOM
// END CUSTOM
