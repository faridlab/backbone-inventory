//! Auth configuration for StockTransferItem handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockTransferItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockTransferItemService;
use crate::domain::entity::StockTransferItem;

/// Permission string constants for StockTransferItem operations.
pub mod permissions {
    pub const LIST:        &str = "stock_transfer_item:list";
    pub const READ:        &str = "stock_transfer_item:read";
    pub const CREATE:      &str = "stock_transfer_item:create";
    pub const UPDATE:      &str = "stock_transfer_item:update";
    pub const DELETE:      &str = "stock_transfer_item:delete";
    pub const RESTORE:     &str = "stock_transfer_item:restore";
    pub const TRASH:       &str = "stock_transfer_item:trash";
    pub const EMPTY_TRASH: &str = "stock_transfer_item:empty_trash";
    pub const BULK_CREATE: &str = "stock_transfer_item:bulk_create";
    pub const UPSERT:      &str = "stock_transfer_item:upsert";
}

/// Resource policy for StockTransferItem — maps CRUD actions to permission strings.
pub struct StockTransferItemPolicy;

#[async_trait]
impl ResourcePolicy<StockTransferItem> for StockTransferItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockTransferItem,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for StockTransferItem handlers.
///
/// Wraps `StockTransferItemService` and checks `StockTransferItemPolicy` before delegating.
pub type StockTransferItemGuard = PermissionGuard<StockTransferItem, StockTransferItemService, StockTransferItemPolicy>;

/// Service-integrated permission guard for StockTransferItem (for use in generated handlers).
pub type StockTransferItemServiceGuard = ServicePermissionGuard<StockTransferItem, StockTransferItemService, StockTransferItemPolicy>;

// <<< CUSTOM
// END CUSTOM
