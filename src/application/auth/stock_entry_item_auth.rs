//! Auth configuration for StockEntryItem handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockEntryItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockEntryItemService;
use crate::domain::entity::StockEntryItem;

/// Permission string constants for StockEntryItem operations.
pub mod permissions {
    pub const LIST:        &str = "stock_entry_item:list";
    pub const READ:        &str = "stock_entry_item:read";
    pub const CREATE:      &str = "stock_entry_item:create";
    pub const UPDATE:      &str = "stock_entry_item:update";
    pub const DELETE:      &str = "stock_entry_item:delete";
    pub const RESTORE:     &str = "stock_entry_item:restore";
    pub const TRASH:       &str = "stock_entry_item:trash";
    pub const EMPTY_TRASH: &str = "stock_entry_item:empty_trash";
    pub const BULK_CREATE: &str = "stock_entry_item:bulk_create";
    pub const UPSERT:      &str = "stock_entry_item:upsert";
}

/// Resource policy for StockEntryItem — maps CRUD actions to permission strings.
pub struct StockEntryItemPolicy;

#[async_trait]
impl ResourcePolicy<StockEntryItem> for StockEntryItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockEntryItem,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for StockEntryItem handlers.
///
/// Wraps `StockEntryItemService` and checks `StockEntryItemPolicy` before delegating.
pub type StockEntryItemGuard = PermissionGuard<StockEntryItem>;

/// Service-integrated permission guard for StockEntryItem (for use in generated handlers).
pub type StockEntryItemServiceGuard = ServicePermissionGuard<StockEntryItem, StockEntryItemService, StockEntryItemPolicy>;

// <<< CUSTOM
// END CUSTOM
