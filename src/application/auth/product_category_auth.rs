//! Auth configuration for ProductCategory handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ProductCategory>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ProductCategoryService;
use crate::domain::entity::ProductCategory;

/// Permission string constants for ProductCategory operations.
pub mod permissions {
    pub const LIST:        &str = "product_category:list";
    pub const READ:        &str = "product_category:read";
    pub const CREATE:      &str = "product_category:create";
    pub const UPDATE:      &str = "product_category:update";
    pub const DELETE:      &str = "product_category:delete";
    pub const RESTORE:     &str = "product_category:restore";
    pub const TRASH:       &str = "product_category:trash";
    pub const EMPTY_TRASH: &str = "product_category:empty_trash";
    pub const BULK_CREATE: &str = "product_category:bulk_create";
    pub const UPSERT:      &str = "product_category:upsert";
}

/// Resource policy for ProductCategory — maps CRUD actions to permission strings.
pub struct ProductCategoryPolicy;

#[async_trait]
impl ResourcePolicy<ProductCategory> for ProductCategoryPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ProductCategory,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for ProductCategory handlers.
///
/// Wraps `ProductCategoryService` and checks `ProductCategoryPolicy` before delegating.
pub type ProductCategoryGuard = PermissionGuard<ProductCategory, ProductCategoryService, ProductCategoryPolicy>;

/// Service-integrated permission guard for ProductCategory (for use in generated handlers).
pub type ProductCategoryServiceGuard = ServicePermissionGuard<ProductCategory, ProductCategoryService, ProductCategoryPolicy>;

// <<< CUSTOM
// END CUSTOM
