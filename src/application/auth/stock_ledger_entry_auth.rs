//! Auth configuration for StockLedgerEntry handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockLedgerEntry>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockLedgerEntryService;
use crate::domain::entity::StockLedgerEntry;

/// Permission string constants for StockLedgerEntry operations.
pub mod permissions {
    pub const LIST:        &str = "stock_ledger_entry:list";
    pub const READ:        &str = "stock_ledger_entry:read";
    pub const CREATE:      &str = "stock_ledger_entry:create";
    pub const UPDATE:      &str = "stock_ledger_entry:update";
    pub const DELETE:      &str = "stock_ledger_entry:delete";
    pub const RESTORE:     &str = "stock_ledger_entry:restore";
    pub const TRASH:       &str = "stock_ledger_entry:trash";
    pub const EMPTY_TRASH: &str = "stock_ledger_entry:empty_trash";
    pub const BULK_CREATE: &str = "stock_ledger_entry:bulk_create";
    pub const UPSERT:      &str = "stock_ledger_entry:upsert";
}

/// Resource policy for StockLedgerEntry — maps CRUD actions to permission strings.
pub struct StockLedgerEntryPolicy;

#[async_trait]
impl ResourcePolicy<StockLedgerEntry> for StockLedgerEntryPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockLedgerEntry,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for StockLedgerEntry handlers.
///
/// Wraps `StockLedgerEntryService` and checks `StockLedgerEntryPolicy` before delegating.
pub type StockLedgerEntryGuard = PermissionGuard<StockLedgerEntry>;

/// Service-integrated permission guard for StockLedgerEntry (for use in generated handlers).
pub type StockLedgerEntryServiceGuard = ServicePermissionGuard<StockLedgerEntry, StockLedgerEntryService, StockLedgerEntryPolicy>;

// <<< CUSTOM
// END CUSTOM
