//! Auth configuration for Product handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Product>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ProductService;
use crate::domain::entity::Product;

/// Permission string constants for Product operations.
pub mod permissions {
    pub const LIST:        &str = "product:list";
    pub const READ:        &str = "product:read";
    pub const CREATE:      &str = "product:create";
    pub const UPDATE:      &str = "product:update";
    pub const DELETE:      &str = "product:delete";
    pub const RESTORE:     &str = "product:restore";
    pub const TRASH:       &str = "product:trash";
    pub const EMPTY_TRASH: &str = "product:empty_trash";
    pub const BULK_CREATE: &str = "product:bulk_create";
    pub const UPSERT:      &str = "product:upsert";
}

/// Resource policy for Product — maps CRUD actions to permission strings.
pub struct ProductPolicy;

#[async_trait]
impl ResourcePolicy<Product> for ProductPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Product,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Product handlers.
///
/// Wraps `ProductService` and checks `ProductPolicy` before delegating.
pub type ProductGuard = PermissionGuard<Product, ProductService, ProductPolicy>;

/// Service-integrated permission guard for Product (for use in generated handlers).
pub type ProductServiceGuard = ServicePermissionGuard<Product, ProductService, ProductPolicy>;

// <<< CUSTOM
// END CUSTOM
