//! Auth configuration for GoodsReceiptItem handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<GoodsReceiptItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::GoodsReceiptItemService;
use crate::domain::entity::GoodsReceiptItem;

/// Permission string constants for GoodsReceiptItem operations.
pub mod permissions {
    pub const LIST:        &str = "goods_receipt_item:list";
    pub const READ:        &str = "goods_receipt_item:read";
    pub const CREATE:      &str = "goods_receipt_item:create";
    pub const UPDATE:      &str = "goods_receipt_item:update";
    pub const DELETE:      &str = "goods_receipt_item:delete";
    pub const RESTORE:     &str = "goods_receipt_item:restore";
    pub const TRASH:       &str = "goods_receipt_item:trash";
    pub const EMPTY_TRASH: &str = "goods_receipt_item:empty_trash";
    pub const BULK_CREATE: &str = "goods_receipt_item:bulk_create";
    pub const UPSERT:      &str = "goods_receipt_item:upsert";
}

/// Resource policy for GoodsReceiptItem — maps CRUD actions to permission strings.
pub struct GoodsReceiptItemPolicy;

#[async_trait]
impl ResourcePolicy<GoodsReceiptItem> for GoodsReceiptItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &GoodsReceiptItem,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for GoodsReceiptItem handlers.
///
/// Wraps `GoodsReceiptItemService` and checks `GoodsReceiptItemPolicy` before delegating.
pub type GoodsReceiptItemGuard = PermissionGuard<GoodsReceiptItem, GoodsReceiptItemService, GoodsReceiptItemPolicy>;

/// Service-integrated permission guard for GoodsReceiptItem (for use in generated handlers).
pub type GoodsReceiptItemServiceGuard = ServicePermissionGuard<GoodsReceiptItem, GoodsReceiptItemService, GoodsReceiptItemPolicy>;

// <<< CUSTOM
// END CUSTOM
