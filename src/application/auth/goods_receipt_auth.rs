//! Auth configuration for GoodsReceipt handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<GoodsReceipt>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::GoodsReceiptService;
use crate::domain::entity::GoodsReceipt;

/// Permission string constants for GoodsReceipt operations.
pub mod permissions {
    pub const LIST:        &str = "goods_receipt:list";
    pub const READ:        &str = "goods_receipt:read";
    pub const CREATE:      &str = "goods_receipt:create";
    pub const UPDATE:      &str = "goods_receipt:update";
    pub const DELETE:      &str = "goods_receipt:delete";
    pub const RESTORE:     &str = "goods_receipt:restore";
    pub const TRASH:       &str = "goods_receipt:trash";
    pub const EMPTY_TRASH: &str = "goods_receipt:empty_trash";
    pub const BULK_CREATE: &str = "goods_receipt:bulk_create";
    pub const UPSERT:      &str = "goods_receipt:upsert";
}

/// Resource policy for GoodsReceipt — maps CRUD actions to permission strings.
pub struct GoodsReceiptPolicy;

#[async_trait]
impl ResourcePolicy<GoodsReceipt> for GoodsReceiptPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &GoodsReceipt,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for GoodsReceipt handlers.
///
/// Wraps `GoodsReceiptService` and checks `GoodsReceiptPolicy` before delegating.
pub type GoodsReceiptGuard = PermissionGuard<GoodsReceipt, GoodsReceiptService, GoodsReceiptPolicy>;

/// Service-integrated permission guard for GoodsReceipt (for use in generated handlers).
pub type GoodsReceiptServiceGuard = ServicePermissionGuard<GoodsReceipt, GoodsReceiptService, GoodsReceiptPolicy>;

// <<< CUSTOM
// END CUSTOM
