//! Auth configuration for StockBatch handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockBatch>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockBatchService;
use crate::domain::entity::StockBatch;

/// Permission string constants for StockBatch operations.
pub mod permissions {
    pub const LIST:        &str = "stock_batch:list";
    pub const READ:        &str = "stock_batch:read";
    pub const CREATE:      &str = "stock_batch:create";
    pub const UPDATE:      &str = "stock_batch:update";
    pub const DELETE:      &str = "stock_batch:delete";
    pub const RESTORE:     &str = "stock_batch:restore";
    pub const TRASH:       &str = "stock_batch:trash";
    pub const EMPTY_TRASH: &str = "stock_batch:empty_trash";
    pub const BULK_CREATE: &str = "stock_batch:bulk_create";
    pub const UPSERT:      &str = "stock_batch:upsert";
}

/// Resource policy for StockBatch — maps CRUD actions to permission strings.
pub struct StockBatchPolicy;

#[async_trait]
impl ResourcePolicy<StockBatch> for StockBatchPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockBatch,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for StockBatch handlers.
///
/// Wraps `StockBatchService` and checks `StockBatchPolicy` before delegating.
pub type StockBatchGuard = PermissionGuard<StockBatch, StockBatchService, StockBatchPolicy>;

/// Service-integrated permission guard for StockBatch (for use in generated handlers).
pub type StockBatchServiceGuard = ServicePermissionGuard<StockBatch, StockBatchService, StockBatchPolicy>;

// <<< CUSTOM
// END CUSTOM
