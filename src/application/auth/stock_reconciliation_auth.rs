//! Auth configuration for StockReconciliation handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<StockReconciliation>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockReconciliationService;
use crate::domain::entity::StockReconciliation;

/// Permission string constants for StockReconciliation operations.
pub mod permissions {
    pub const LIST:        &str = "stock_reconciliation:list";
    pub const READ:        &str = "stock_reconciliation:read";
    pub const CREATE:      &str = "stock_reconciliation:create";
    pub const UPDATE:      &str = "stock_reconciliation:update";
    pub const DELETE:      &str = "stock_reconciliation:delete";
    pub const RESTORE:     &str = "stock_reconciliation:restore";
    pub const TRASH:       &str = "stock_reconciliation:trash";
    pub const EMPTY_TRASH: &str = "stock_reconciliation:empty_trash";
    pub const BULK_CREATE: &str = "stock_reconciliation:bulk_create";
    pub const UPSERT:      &str = "stock_reconciliation:upsert";
}

/// Resource policy for StockReconciliation — maps CRUD actions to permission strings.
pub struct StockReconciliationPolicy;

#[async_trait]
impl ResourcePolicy<StockReconciliation> for StockReconciliationPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &StockReconciliation,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for StockReconciliation handlers.
///
/// Wraps `StockReconciliationService` and checks `StockReconciliationPolicy` before delegating.
pub type StockReconciliationGuard = PermissionGuard<StockReconciliation>;

/// Service-integrated permission guard for StockReconciliation (for use in generated handlers).
pub type StockReconciliationServiceGuard = ServicePermissionGuard<StockReconciliation, StockReconciliationService, StockReconciliationPolicy>;

// <<< CUSTOM
// END CUSTOM
