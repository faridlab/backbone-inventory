//! Auth configuration for Stock handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Stock>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::StockService;
use crate::domain::entity::Stock;

/// Permission string constants for Stock operations.
pub mod permissions {
    pub const LIST:        &str = "stock:list";
    pub const READ:        &str = "stock:read";
    pub const CREATE:      &str = "stock:create";
    pub const UPDATE:      &str = "stock:update";
    pub const DELETE:      &str = "stock:delete";
    pub const RESTORE:     &str = "stock:restore";
    pub const TRASH:       &str = "stock:trash";
    pub const EMPTY_TRASH: &str = "stock:empty_trash";
    pub const BULK_CREATE: &str = "stock:bulk_create";
    pub const UPSERT:      &str = "stock:upsert";
}

/// Resource policy for Stock — maps CRUD actions to permission strings.
pub struct StockPolicy;

#[async_trait]
impl ResourcePolicy<Stock> for StockPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Stock,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Stock handlers.
///
/// Wraps `StockService` and checks `StockPolicy` before delegating.
pub type StockGuard = PermissionGuard<Stock, StockService, StockPolicy>;

/// Service-integrated permission guard for Stock (for use in generated handlers).
pub type StockServiceGuard = ServicePermissionGuard<Stock, StockService, StockPolicy>;

// <<< CUSTOM
// END CUSTOM
