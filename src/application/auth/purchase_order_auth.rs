//! Auth configuration for PurchaseOrder handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PurchaseOrder>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PurchaseOrderService;
use crate::domain::entity::PurchaseOrder;

/// Permission string constants for PurchaseOrder operations.
pub mod permissions {
    pub const LIST:        &str = "purchase_order:list";
    pub const READ:        &str = "purchase_order:read";
    pub const CREATE:      &str = "purchase_order:create";
    pub const UPDATE:      &str = "purchase_order:update";
    pub const DELETE:      &str = "purchase_order:delete";
    pub const RESTORE:     &str = "purchase_order:restore";
    pub const TRASH:       &str = "purchase_order:trash";
    pub const EMPTY_TRASH: &str = "purchase_order:empty_trash";
    pub const BULK_CREATE: &str = "purchase_order:bulk_create";
    pub const UPSERT:      &str = "purchase_order:upsert";
}

/// Resource policy for PurchaseOrder — maps CRUD actions to permission strings.
pub struct PurchaseOrderPolicy;

#[async_trait]
impl ResourcePolicy<PurchaseOrder> for PurchaseOrderPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PurchaseOrder,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PurchaseOrder handlers.
///
/// Wraps `PurchaseOrderService` and checks `PurchaseOrderPolicy` before delegating.
pub type PurchaseOrderGuard = PermissionGuard<PurchaseOrder, PurchaseOrderService, PurchaseOrderPolicy>;

/// Service-integrated permission guard for PurchaseOrder (for use in generated handlers).
pub type PurchaseOrderServiceGuard = ServicePermissionGuard<PurchaseOrder, PurchaseOrderService, PurchaseOrderPolicy>;

// <<< CUSTOM
// END CUSTOM
