//! Repository for LandedCostLine entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<LandedCostLine, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::LandedCostLine;

/// Table name for LandedCostLine entities
pub const TABLE_NAME: &str = "inventory.landed_cost_lines";

/// Repository for LandedCostLine entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct LandedCostLineRepository(
    backbone_orm::GenericCrudRepository<LandedCostLine, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for LandedCostLineRepository {
    type Target = backbone_orm::GenericCrudRepository<LandedCostLine, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl LandedCostLineRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(LandedCostLineRepository, LandedCostLine, soft_delete);
