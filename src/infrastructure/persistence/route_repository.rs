//! Repository for Route entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Route, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Route;

/// Table name for Route entities
pub const TABLE_NAME: &str = "inventory.routes";

/// Repository for Route entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct RouteRepository(
    backbone_orm::GenericCrudRepository<Route, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for RouteRepository {
    type Target = backbone_orm::GenericCrudRepository<Route, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl RouteRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(RouteRepository, Route, soft_delete);
