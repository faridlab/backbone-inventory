//! Repository for Stock entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Stock, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Stock;

/// Table name for Stock entities
pub const TABLE_NAME: &str = "stocks";

/// Repository for Stock entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct StockRepository(
    backbone_orm::GenericCrudRepository<Stock, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for StockRepository {
    type Target = backbone_orm::GenericCrudRepository<Stock, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl StockRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(StockRepository, Stock, soft_delete);
