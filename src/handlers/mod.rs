//! Inventory Module Handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - AppState dependency injection container
//! - Custom handler integration points

use std::sync::Arc;

// Import all services
use crate::application::service::GoodsReceiptService;
use crate::application::service::GoodsReceiptItemService;
use crate::application::service::ProductCategoryService;
use crate::application::service::ProductService;
use crate::application::service::PurchaseOrderService;
use crate::application::service::PurchaseOrderItemService;
use crate::application::service::StockService;
use crate::application::service::StockAdjustmentService;
use crate::application::service::StockAdjustmentItemService;
use crate::application::service::StockBatchService;
use crate::application::service::StockMovementService;
use crate::application::service::StockTransferService;
use crate::application::service::StockTransferItemService;
use crate::application::service::WarehouseService;

/// Application state for dependency injection.
///
/// This struct holds references to all module services
/// and is passed to HTTP handlers via Axum's State extractor.
///
/// # Example
///
/// ```ignore
/// use axum::extract::State;
///
/// async fn custom_handler(
///     State(state): State<AppState>,
/// ) -> impl IntoResponse {
///     // Access services via state
/// }
/// ```
#[derive(Clone)]
pub struct AppState {
    /// GoodsReceipt service
    pub goods_receipt_service: Arc<GoodsReceiptService>,
    /// GoodsReceiptItem service
    pub goods_receipt_item_service: Arc<GoodsReceiptItemService>,
    /// ProductCategory service
    pub product_category_service: Arc<ProductCategoryService>,
    /// Product service
    pub product_service: Arc<ProductService>,
    /// PurchaseOrder service
    pub purchase_order_service: Arc<PurchaseOrderService>,
    /// PurchaseOrderItem service
    pub purchase_order_item_service: Arc<PurchaseOrderItemService>,
    /// Stock service
    pub stock_service: Arc<StockService>,
    /// StockAdjustment service
    pub stock_adjustment_service: Arc<StockAdjustmentService>,
    /// StockAdjustmentItem service
    pub stock_adjustment_item_service: Arc<StockAdjustmentItemService>,
    /// StockBatch service
    pub stock_batch_service: Arc<StockBatchService>,
    /// StockMovement service
    pub stock_movement_service: Arc<StockMovementService>,
    /// StockTransfer service
    pub stock_transfer_service: Arc<StockTransferService>,
    /// StockTransferItem service
    pub stock_transfer_item_service: Arc<StockTransferItemService>,
    /// Warehouse service
    pub warehouse_service: Arc<WarehouseService>,
}

impl AppState {
    /// Create a new AppState with all services.
    pub fn new(
        goods_receipt_service: Arc<GoodsReceiptService>,
        goods_receipt_item_service: Arc<GoodsReceiptItemService>,
        product_category_service: Arc<ProductCategoryService>,
        product_service: Arc<ProductService>,
        purchase_order_service: Arc<PurchaseOrderService>,
        purchase_order_item_service: Arc<PurchaseOrderItemService>,
        stock_service: Arc<StockService>,
        stock_adjustment_service: Arc<StockAdjustmentService>,
        stock_adjustment_item_service: Arc<StockAdjustmentItemService>,
        stock_batch_service: Arc<StockBatchService>,
        stock_movement_service: Arc<StockMovementService>,
        stock_transfer_service: Arc<StockTransferService>,
        stock_transfer_item_service: Arc<StockTransferItemService>,
        warehouse_service: Arc<WarehouseService>
    ) -> Self {
        Self {
            goods_receipt_service,
            goods_receipt_item_service,
            product_category_service,
            product_service,
            purchase_order_service,
            purchase_order_item_service,
            stock_service,
            stock_adjustment_service,
            stock_adjustment_item_service,
            stock_batch_service,
            stock_movement_service,
            stock_transfer_service,
            stock_transfer_item_service,
            warehouse_service,
        }
    }

    /// Create AppState from module instance.
    pub fn from_module(module: &crate::InventoryModule) -> Self {
        Self {
            goods_receipt_service: module.goods_receipt_service.clone(),
            goods_receipt_item_service: module.goods_receipt_item_service.clone(),
            product_category_service: module.product_category_service.clone(),
            product_service: module.product_service.clone(),
            purchase_order_service: module.purchase_order_service.clone(),
            purchase_order_item_service: module.purchase_order_item_service.clone(),
            stock_service: module.stock_service.clone(),
            stock_adjustment_service: module.stock_adjustment_service.clone(),
            stock_adjustment_item_service: module.stock_adjustment_item_service.clone(),
            stock_batch_service: module.stock_batch_service.clone(),
            stock_movement_service: module.stock_movement_service.clone(),
            stock_transfer_service: module.stock_transfer_service.clone(),
            stock_transfer_item_service: module.stock_transfer_item_service.clone(),
            warehouse_service: module.warehouse_service.clone(),
        }
    }
}

/// Builder for AppState.
///
/// Allows incremental construction of AppState.
#[derive(Default)]
pub struct AppStateBuilder {
    goods_receipt_service: Option<Arc<GoodsReceiptService>>,
    goods_receipt_item_service: Option<Arc<GoodsReceiptItemService>>,
    product_category_service: Option<Arc<ProductCategoryService>>,
    product_service: Option<Arc<ProductService>>,
    purchase_order_service: Option<Arc<PurchaseOrderService>>,
    purchase_order_item_service: Option<Arc<PurchaseOrderItemService>>,
    stock_service: Option<Arc<StockService>>,
    stock_adjustment_service: Option<Arc<StockAdjustmentService>>,
    stock_adjustment_item_service: Option<Arc<StockAdjustmentItemService>>,
    stock_batch_service: Option<Arc<StockBatchService>>,
    stock_movement_service: Option<Arc<StockMovementService>>,
    stock_transfer_service: Option<Arc<StockTransferService>>,
    stock_transfer_item_service: Option<Arc<StockTransferItemService>>,
    warehouse_service: Option<Arc<WarehouseService>>,
}

impl AppStateBuilder {
    /// Create a new builder.
    pub fn new() -> Self {
        Self::default()
    }

    /// Set the GoodsReceipt service.
    pub fn with_goods_receipt_service(mut self, service: Arc<GoodsReceiptService>) -> Self {
        self.goods_receipt_service = Some(service);
        self
    }

    /// Set the GoodsReceiptItem service.
    pub fn with_goods_receipt_item_service(mut self, service: Arc<GoodsReceiptItemService>) -> Self {
        self.goods_receipt_item_service = Some(service);
        self
    }

    /// Set the ProductCategory service.
    pub fn with_product_category_service(mut self, service: Arc<ProductCategoryService>) -> Self {
        self.product_category_service = Some(service);
        self
    }

    /// Set the Product service.
    pub fn with_product_service(mut self, service: Arc<ProductService>) -> Self {
        self.product_service = Some(service);
        self
    }

    /// Set the PurchaseOrder service.
    pub fn with_purchase_order_service(mut self, service: Arc<PurchaseOrderService>) -> Self {
        self.purchase_order_service = Some(service);
        self
    }

    /// Set the PurchaseOrderItem service.
    pub fn with_purchase_order_item_service(mut self, service: Arc<PurchaseOrderItemService>) -> Self {
        self.purchase_order_item_service = Some(service);
        self
    }

    /// Set the Stock service.
    pub fn with_stock_service(mut self, service: Arc<StockService>) -> Self {
        self.stock_service = Some(service);
        self
    }

    /// Set the StockAdjustment service.
    pub fn with_stock_adjustment_service(mut self, service: Arc<StockAdjustmentService>) -> Self {
        self.stock_adjustment_service = Some(service);
        self
    }

    /// Set the StockAdjustmentItem service.
    pub fn with_stock_adjustment_item_service(mut self, service: Arc<StockAdjustmentItemService>) -> Self {
        self.stock_adjustment_item_service = Some(service);
        self
    }

    /// Set the StockBatch service.
    pub fn with_stock_batch_service(mut self, service: Arc<StockBatchService>) -> Self {
        self.stock_batch_service = Some(service);
        self
    }

    /// Set the StockMovement service.
    pub fn with_stock_movement_service(mut self, service: Arc<StockMovementService>) -> Self {
        self.stock_movement_service = Some(service);
        self
    }

    /// Set the StockTransfer service.
    pub fn with_stock_transfer_service(mut self, service: Arc<StockTransferService>) -> Self {
        self.stock_transfer_service = Some(service);
        self
    }

    /// Set the StockTransferItem service.
    pub fn with_stock_transfer_item_service(mut self, service: Arc<StockTransferItemService>) -> Self {
        self.stock_transfer_item_service = Some(service);
        self
    }

    /// Set the Warehouse service.
    pub fn with_warehouse_service(mut self, service: Arc<WarehouseService>) -> Self {
        self.warehouse_service = Some(service);
        self
    }

    /// Build the AppState.
    ///
    /// # Panics
    ///
    /// Panics if any required service is not set.
    pub fn build(self) -> AppState {
        AppState {
            goods_receipt_service: self.goods_receipt_service.expect("goods_receipt_service is required"),
            goods_receipt_item_service: self.goods_receipt_item_service.expect("goods_receipt_item_service is required"),
            product_category_service: self.product_category_service.expect("product_category_service is required"),
            product_service: self.product_service.expect("product_service is required"),
            purchase_order_service: self.purchase_order_service.expect("purchase_order_service is required"),
            purchase_order_item_service: self.purchase_order_item_service.expect("purchase_order_item_service is required"),
            stock_service: self.stock_service.expect("stock_service is required"),
            stock_adjustment_service: self.stock_adjustment_service.expect("stock_adjustment_service is required"),
            stock_adjustment_item_service: self.stock_adjustment_item_service.expect("stock_adjustment_item_service is required"),
            stock_batch_service: self.stock_batch_service.expect("stock_batch_service is required"),
            stock_movement_service: self.stock_movement_service.expect("stock_movement_service is required"),
            stock_transfer_service: self.stock_transfer_service.expect("stock_transfer_service is required"),
            stock_transfer_item_service: self.stock_transfer_item_service.expect("stock_transfer_item_service is required"),
            warehouse_service: self.warehouse_service.expect("warehouse_service is required"),
        }
    }
}
