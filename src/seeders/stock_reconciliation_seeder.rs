//! StockReconciliation seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial StockReconciliation data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// STOCKRECONCILIATION SEEDER
// ============================================================================

/// Seeder for stock_reconciliation
pub struct SeedStockReconciliationSeeder;

impl SeedStockReconciliationSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedStockReconciliationSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedStockReconciliationSeeder {
    fn name(&self) -> &'static str {
        "SeedStockReconciliationSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.stock_reconciliations table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.stock_reconciliations")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/stock_reconciliation_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.stock_reconciliations")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

