//! ScrapReasonTag seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ScrapReasonTag data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SCRAPREASONTAG SEEDER
// ============================================================================

/// Seeder for scrap_reason_tag
pub struct SeedScrapReasonTagSeeder;

impl SeedScrapReasonTagSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedScrapReasonTagSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedScrapReasonTagSeeder {
    fn name(&self) -> &'static str {
        "SeedScrapReasonTagSeeder"
    }

    fn order(&self) -> i32 {
        20
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.scrap_reason_tags table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.scrap_reason_tags")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/scrap_reason_tag_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.scrap_reason_tags")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
