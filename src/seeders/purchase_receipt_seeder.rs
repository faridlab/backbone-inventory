//! PurchaseReceipt seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial PurchaseReceipt data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PURCHASERECEIPT SEEDER
// ============================================================================

/// Seeder for purchase_receipt
pub struct SeedPurchaseReceiptSeeder;

impl SeedPurchaseReceiptSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPurchaseReceiptSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPurchaseReceiptSeeder {
    fn name(&self) -> &'static str {
        "SeedPurchaseReceiptSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.purchase_receipts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.purchase_receipts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/purchase_receipt_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.purchase_receipts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

