//! Route seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Route data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ROUTE SEEDER
// ============================================================================

/// Seeder for route
pub struct SeedRouteSeeder;

impl SeedRouteSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRouteSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRouteSeeder {
    fn name(&self) -> &'static str {
        "SeedRouteSeeder"
    }

    fn order(&self) -> i32 {
        8
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.routes table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.routes")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/route_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.routes")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
