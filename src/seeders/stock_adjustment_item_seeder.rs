//! StockAdjustmentItem seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial StockAdjustmentItem data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// STOCKADJUSTMENTITEM SEEDER
// ============================================================================

/// Seeder for stock_adjustment_item
pub struct SeedStockAdjustmentItemSeeder;

impl SeedStockAdjustmentItemSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedStockAdjustmentItemSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedStockAdjustmentItemSeeder {
    fn name(&self) -> &'static str {
        "SeedStockAdjustmentItemSeeder"
    }

    fn order(&self) -> i32 {
        8
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if stock_adjustment_items table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM stock_adjustment_items")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/stock_adjustment_item_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM stock_adjustment_items")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

