//! PurchaseOrder seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial PurchaseOrder data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PURCHASEORDER SEEDER
// ============================================================================

/// Seeder for purchase_order
pub struct SeedPurchaseOrderSeeder;

impl SeedPurchaseOrderSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPurchaseOrderSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPurchaseOrderSeeder {
    fn name(&self) -> &'static str {
        "SeedPurchaseOrderSeeder"
    }

    fn order(&self) -> i32 {
        4
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if purchase_orders table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM purchase_orders")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/purchase_order_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM purchase_orders")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

