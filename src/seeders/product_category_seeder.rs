//! ProductCategory seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial ProductCategory data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PRODUCTCATEGORY SEEDER
// ============================================================================

/// Seeder for product_category
pub struct SeedProductCategorySeeder;

impl SeedProductCategorySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedProductCategorySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedProductCategorySeeder {
    fn name(&self) -> &'static str {
        "SeedProductCategorySeeder"
    }

    fn order(&self) -> i32 {
        14
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if product_categories table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM product_categories")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/product_category_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM product_categories")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

