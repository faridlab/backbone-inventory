//! StockLedgerEntry seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial StockLedgerEntry data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// STOCKLEDGERENTRY SEEDER
// ============================================================================

/// Seeder for stock_ledger_entry
pub struct SeedStockLedgerEntrySeeder;

impl SeedStockLedgerEntrySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedStockLedgerEntrySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedStockLedgerEntrySeeder {
    fn name(&self) -> &'static str {
        "SeedStockLedgerEntrySeeder"
    }

    fn order(&self) -> i32 {
        7
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.stock_ledger_entries table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.stock_ledger_entries")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/stock_ledger_entry_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.stock_ledger_entries")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

