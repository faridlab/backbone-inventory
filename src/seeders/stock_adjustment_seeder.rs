//! StockAdjustment seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial StockAdjustment data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// STOCKADJUSTMENT SEEDER
// ============================================================================

/// Seeder for stock_adjustment
pub struct SeedStockAdjustmentSeeder;

impl SeedStockAdjustmentSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedStockAdjustmentSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedStockAdjustmentSeeder {
    fn name(&self) -> &'static str {
        "SeedStockAdjustmentSeeder"
    }

    fn order(&self) -> i32 {
        7
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if stock_adjustments table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM stock_adjustments")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/stock_adjustment_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM stock_adjustments")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

