//! PackageType seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial PackageType data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PACKAGETYPE SEEDER
// ============================================================================

/// Seeder for package_type
pub struct SeedPackageTypeSeeder;

impl SeedPackageTypeSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPackageTypeSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPackageTypeSeeder {
    fn name(&self) -> &'static str {
        "SeedPackageTypeSeeder"
    }

    fn order(&self) -> i32 {
        27
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.package_types table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.package_types")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/package_type_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.package_types")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
