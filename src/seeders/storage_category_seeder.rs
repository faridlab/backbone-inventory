//! StorageCategory seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial StorageCategory data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// STORAGECATEGORY SEEDER
// ============================================================================

/// Seeder for storage_category
pub struct SeedStorageCategorySeeder;

impl SeedStorageCategorySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedStorageCategorySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedStorageCategorySeeder {
    fn name(&self) -> &'static str {
        "SeedStorageCategorySeeder"
    }

    fn order(&self) -> i32 {
        28
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.storage_categories table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.storage_categories")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/storage_category_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.storage_categories")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
