//! ReorderingRule seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ReorderingRule data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// REORDERINGRULE SEEDER
// ============================================================================

/// Seeder for reordering_rule
pub struct SeedReorderingRuleSeeder;

impl SeedReorderingRuleSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedReorderingRuleSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedReorderingRuleSeeder {
    fn name(&self) -> &'static str {
        "SeedReorderingRuleSeeder"
    }

    fn order(&self) -> i32 {
        15
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.reordering_rules table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.reordering_rules")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/reordering_rule_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.reordering_rules")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
