//! RouteRule seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RouteRule data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ROUTERULE SEEDER
// ============================================================================

/// Seeder for route_rule
pub struct SeedRouteRuleSeeder;

impl SeedRouteRuleSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRouteRuleSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRouteRuleSeeder {
    fn name(&self) -> &'static str {
        "SeedRouteRuleSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if inventory.route_rules table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM inventory.route_rules")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/route_rule_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM inventory.route_rules")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
