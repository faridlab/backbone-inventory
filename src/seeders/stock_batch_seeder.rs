//! StockBatch seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial StockBatch data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// STOCKBATCH SEEDER
// ============================================================================

/// Seeder for stock_batch
pub struct SeedStockBatchSeeder;

impl SeedStockBatchSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedStockBatchSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedStockBatchSeeder {
    fn name(&self) -> &'static str {
        "SeedStockBatchSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if stock_batches table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM stock_batches")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/stock_batch_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM stock_batches")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

