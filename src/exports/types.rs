//! Public types for Inventory module
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! These DTOs are the ONLY types other modules should use.
//! They are decoupled from internal domain entities.

use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};

// ============================================================================
// GOODSRECEIPT TYPES
// ============================================================================

/// Type-safe ID for GoodsReceipt
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct GoodsReceiptId(pub Uuid);

impl GoodsReceiptId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for GoodsReceiptId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<GoodsReceiptId> for Uuid {
    fn from(id: GoodsReceiptId) -> Self {
        id.0
    }
}

/// Data transfer object for GoodsReceipt
///
/// This is the public representation of GoodsReceipt for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoodsReceiptDto {
    pub id: GoodsReceiptId,
    pub provider_id: Uuid,
    pub receipt_number: String,
    pub purchase_order_id: Option<Uuid>,
    pub supplier_name: String,
    pub delivery_note: Option<String>,
    pub warehouse_id: Uuid,
    pub receipt_date: NaiveDate,
    pub currency: String,
    pub requires_qc: bool,
    pub qc_date: Option<DateTime<Utc>>,
    pub qc_by: Option<Uuid>,
    pub qc_notes: Option<String>,
    pub status: GoodsReceiptStatus,
    pub received_by: Option<Uuid>,
    pub received_at: Option<DateTime<Utc>>,
    pub posted_at: Option<DateTime<Utc>>,
    pub posted_by: Option<Uuid>,
    pub cancelled_at: Option<DateTime<Utc>>,
    pub cancelled_by: Option<Uuid>,
    pub cancellation_reason: Option<String>,
    pub attachments: serde_json::Value,
    pub notes: Option<String>,
    pub data: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Summary view of GoodsReceipt for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoodsReceiptSummary {
    pub id: GoodsReceiptId,
    pub supplier_name: String,
    pub status: GoodsReceiptStatus,
}

/// Reference to GoodsReceipt for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoodsReceiptRef {
    pub id: GoodsReceiptId,
}

// ============================================================================
// GOODSRECEIPTITEM TYPES
// ============================================================================

/// Type-safe ID for GoodsReceiptItem
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct GoodsReceiptItemId(pub Uuid);

impl GoodsReceiptItemId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for GoodsReceiptItemId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<GoodsReceiptItemId> for Uuid {
    fn from(id: GoodsReceiptItemId) -> Self {
        id.0
    }
}

/// Data transfer object for GoodsReceiptItem
///
/// This is the public representation of GoodsReceiptItem for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoodsReceiptItemDto {
    pub id: GoodsReceiptItemId,
    pub goods_receipt_id: Uuid,
    pub product_id: Uuid,
    pub purchase_order_item_id: Option<Uuid>,
    pub ordered_quantity: Decimal,
    pub received_quantity: Decimal,
    pub accepted_quantity: Decimal,
    pub rejected_quantity: Decimal,
    pub variance_quantity: Decimal,
    pub uom: String,
    pub base_uom_quantity: Option<Decimal>,
    pub conversion_factor: Decimal,
    pub unit_cost: Decimal,
    pub total_cost: Decimal,
    pub currency: String,
    pub batch_number: Option<String>,
    pub serial_numbers: serde_json::Value,
    pub manufacture_date: Option<NaiveDate>,
    pub expiry_date: Option<NaiveDate>,
    pub warehouse_id: Option<Uuid>,
    pub bin_location: Option<String>,
    pub requires_qc: bool,
    pub qc_result: QCResult,
    pub qc_date: Option<DateTime<Utc>>,
    pub qc_by: Option<Uuid>,
    pub qc_notes: Option<String>,
    pub qc_parameters: serde_json::Value,
    pub rejection_reason: Option<String>,
    pub rejection_action: Option<String>,
    pub status: ReceiptItemStatus,
    pub stock_movement_id: Option<Uuid>,
    pub is_posted: bool,
    pub posted_at: Option<DateTime<Utc>>,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of GoodsReceiptItem for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoodsReceiptItemSummary {
    pub id: GoodsReceiptItemId,
    pub status: ReceiptItemStatus,
}

/// Reference to GoodsReceiptItem for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GoodsReceiptItemRef {
    pub id: GoodsReceiptItemId,
}

// ============================================================================
// PRODUCTCATEGORY TYPES
// ============================================================================

/// Type-safe ID for ProductCategory
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct ProductCategoryId(pub Uuid);

impl ProductCategoryId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for ProductCategoryId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<ProductCategoryId> for Uuid {
    fn from(id: ProductCategoryId) -> Self {
        id.0
    }
}

/// Data transfer object for ProductCategory
///
/// This is the public representation of ProductCategory for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductCategoryDto {
    pub id: ProductCategoryId,
    pub provider_id: Option<Uuid>,
    pub code: String,
    pub name: String,
    pub description: Option<String>,
    pub parent_id: Option<Uuid>,
    pub level: i32,
    pub path: Option<String>,
    pub sort_order: i32,
    pub default_uom: Option<UnitOfMeasure>,
    pub default_costing_method: Option<CostingMethod>,
    pub default_inventory_account_id: Option<Uuid>,
    pub default_cogs_account_id: Option<Uuid>,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of ProductCategory for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductCategorySummary {
    pub id: ProductCategoryId,
    pub name: String,
}

/// Reference to ProductCategory for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductCategoryRef {
    pub id: ProductCategoryId,
}

// ============================================================================
// PRODUCT TYPES
// ============================================================================

/// Type-safe ID for Product
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct ProductId(pub Uuid);

impl ProductId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for ProductId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<ProductId> for Uuid {
    fn from(id: ProductId) -> Self {
        id.0
    }
}

/// Data transfer object for Product
///
/// This is the public representation of Product for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductDto {
    pub id: ProductId,
    pub provider_id: Option<Uuid>,
    pub sku: String,
    pub barcode: Option<String>,
    pub name: String,
    pub description: Option<String>,
    pub product_type: ProductType,
    pub category_id: Uuid,
    pub brand: Option<String>,
    pub manufacturer: Option<String>,
    pub base_uom: UnitOfMeasure,
    pub purchase_uom: Option<UnitOfMeasure>,
    pub purchase_uom_ratio: Decimal,
    pub secondary_uom: Option<UnitOfMeasure>,
    pub secondary_uom_ratio: Option<Decimal>,
    pub weight_kg: Option<Decimal>,
    pub volume_liter: Option<Decimal>,
    pub costing_method: CostingMethod,
    pub standard_cost: Decimal,
    pub last_purchase_cost: Decimal,
    pub average_cost: Decimal,
    pub currency: String,
    pub is_stockable: bool,
    pub track_batches: bool,
    pub track_expiry: bool,
    pub shelf_life_days: Option<i32>,
    pub expiry_warning_days: Option<i32>,
    pub min_stock: Decimal,
    pub max_stock: Option<Decimal>,
    pub reorder_quantity: Option<Decimal>,
    pub safety_stock: Decimal,
    pub lead_time_days: i32,
    pub usage_per_kg: Option<Decimal>,
    pub usage_per_order: Option<Decimal>,
    pub applicable_services: serde_json::Value,
    pub storage_conditions: Option<String>,
    pub image_url: Option<String>,
    pub status: ProductStatus,
    pub tags: serde_json::Value,
    pub notes: Option<String>,
    pub data: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Summary view of Product for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductSummary {
    pub id: ProductId,
    pub name: String,
    pub status: ProductStatus,
}

/// Reference to Product for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProductRef {
    pub id: ProductId,
}

// ============================================================================
// PURCHASEORDER TYPES
// ============================================================================

/// Type-safe ID for PurchaseOrder
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PurchaseOrderId(pub Uuid);

impl PurchaseOrderId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PurchaseOrderId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PurchaseOrderId> for Uuid {
    fn from(id: PurchaseOrderId) -> Self {
        id.0
    }
}

/// Data transfer object for PurchaseOrder
///
/// This is the public representation of PurchaseOrder for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseOrderDto {
    pub id: PurchaseOrderId,
    pub provider_id: Uuid,
    pub po_number: String,
    pub reference_number: Option<String>,
    pub po_type: PurchaseOrderType,
    pub supplier_id: Option<Uuid>,
    pub supplier_name: String,
    pub supplier_contact_person: Option<String>,
    pub warehouse_id: Uuid,
    pub delivery_address: Option<String>,
    pub order_date: NaiveDate,
    pub expected_date: Option<NaiveDate>,
    pub delivery_date: Option<NaiveDate>,
    pub expiry_date: Option<NaiveDate>,
    pub currency: String,
    pub subtotal: Decimal,
    pub discount_percent: Decimal,
    pub discount_amount: Decimal,
    pub tax_percent: Decimal,
    pub tax_amount: Decimal,
    pub shipping_cost: Decimal,
    pub other_cost: Decimal,
    pub total_amount: Decimal,
    pub payment_terms: Option<String>,
    pub payment_due_date: Option<NaiveDate>,
    pub status: PurchaseOrderStatus,
    pub requires_approval: bool,
    pub submitted_at: Option<DateTime<Utc>>,
    pub submitted_by: Option<Uuid>,
    pub approved_at: Option<DateTime<Utc>>,
    pub approved_by: Option<Uuid>,
    pub rejected_at: Option<DateTime<Utc>>,
    pub rejected_by: Option<Uuid>,
    pub rejection_reason: Option<String>,
    pub cancelled_at: Option<DateTime<Utc>>,
    pub cancelled_by: Option<Uuid>,
    pub cancellation_reason: Option<String>,
    pub completed_at: Option<DateTime<Utc>>,
    pub attachments: serde_json::Value,
    pub supplier_notes: Option<String>,
    pub internal_notes: Option<String>,
    pub data: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Summary view of PurchaseOrder for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseOrderSummary {
    pub id: PurchaseOrderId,
    pub supplier_name: String,
    pub status: PurchaseOrderStatus,
}

/// Reference to PurchaseOrder for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseOrderRef {
    pub id: PurchaseOrderId,
}

// ============================================================================
// PURCHASEORDERITEM TYPES
// ============================================================================

/// Type-safe ID for PurchaseOrderItem
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct PurchaseOrderItemId(pub Uuid);

impl PurchaseOrderItemId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for PurchaseOrderItemId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<PurchaseOrderItemId> for Uuid {
    fn from(id: PurchaseOrderItemId) -> Self {
        id.0
    }
}

/// Data transfer object for PurchaseOrderItem
///
/// This is the public representation of PurchaseOrderItem for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseOrderItemDto {
    pub id: PurchaseOrderItemId,
    pub purchase_order_id: Uuid,
    pub provider_id: Uuid,
    pub line_number: i32,
    pub product_id: Uuid,
    pub description: Option<String>,
    pub uom: UnitOfMeasure,
    pub quantity_ordered: Decimal,
    pub base_uom: UnitOfMeasure,
    pub base_quantity: Decimal,
    pub currency: String,
    pub unit_price: Decimal,
    pub discount_percent: Decimal,
    pub discount_amount: Decimal,
    pub subtotal: Decimal,
    pub tax_percent: Decimal,
    pub tax_amount: Decimal,
    pub total_amount: Decimal,
    pub expected_date: Option<NaiveDate>,
    pub warehouse_id: Option<Uuid>,
    pub is_cancelled: bool,
    pub cancellation_reason: Option<String>,
    pub notes: Option<String>,
    pub data: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Summary view of PurchaseOrderItem for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseOrderItemSummary {
    pub id: PurchaseOrderItemId,
}

/// Reference to PurchaseOrderItem for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PurchaseOrderItemRef {
    pub id: PurchaseOrderItemId,
}

// ============================================================================
// STOCK TYPES
// ============================================================================

/// Type-safe ID for Stock
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockId(pub Uuid);

impl StockId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockId> for Uuid {
    fn from(id: StockId) -> Self {
        id.0
    }
}

/// Data transfer object for Stock
///
/// This is the public representation of Stock for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockDto {
    pub id: StockId,
    pub provider_id: Uuid,
    pub product_id: Uuid,
    pub warehouse_id: Uuid,
    pub quantity_on_hand: Decimal,
    pub quantity_reserved: Decimal,
    pub quantity_on_order: Decimal,
    pub quantity_in_transit: Decimal,
    pub quantity_quarantine: Decimal,
    pub quantity_damaged: Decimal,
    pub currency: String,
    pub unit_cost: Decimal,
    pub total_value: Decimal,
    pub data: serde_json::Value,
    pub bin_location: Option<String>,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of Stock for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockSummary {
    pub id: StockId,
}

/// Reference to Stock for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockRef {
    pub id: StockId,
}

// ============================================================================
// STOCKADJUSTMENT TYPES
// ============================================================================

/// Type-safe ID for StockAdjustment
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockAdjustmentId(pub Uuid);

impl StockAdjustmentId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockAdjustmentId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockAdjustmentId> for Uuid {
    fn from(id: StockAdjustmentId) -> Self {
        id.0
    }
}

/// Data transfer object for StockAdjustment
///
/// This is the public representation of StockAdjustment for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockAdjustmentDto {
    pub id: StockAdjustmentId,
    pub provider_id: Uuid,
    pub adjustment_number: String,
    pub warehouse_id: Uuid,
    pub adjustment_type: AdjustmentType,
    pub adjustment_date: NaiveDate,
    pub is_full_count: bool,
    pub product_filter: serde_json::Value,
    pub total_items: i32,
    pub items_with_variance: i32,
    pub total_system_quantity: Decimal,
    pub total_counted_quantity: Decimal,
    pub total_variance_quantity: Decimal,
    pub currency: String,
    pub total_system_value: Decimal,
    pub total_adjustment_value: Decimal,
    pub status: AdjustmentStatus,
    pub requires_approval: bool,
    pub approved_at: Option<DateTime<Utc>>,
    pub approved_by: Option<Uuid>,
    pub rejection_reason: Option<String>,
    pub posted_at: Option<DateTime<Utc>>,
    pub posted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notes: Option<String>,
    pub data: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Summary view of StockAdjustment for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockAdjustmentSummary {
    pub id: StockAdjustmentId,
    pub status: AdjustmentStatus,
}

/// Reference to StockAdjustment for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockAdjustmentRef {
    pub id: StockAdjustmentId,
}

// ============================================================================
// STOCKADJUSTMENTITEM TYPES
// ============================================================================

/// Type-safe ID for StockAdjustmentItem
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockAdjustmentItemId(pub Uuid);

impl StockAdjustmentItemId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockAdjustmentItemId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockAdjustmentItemId> for Uuid {
    fn from(id: StockAdjustmentItemId) -> Self {
        id.0
    }
}

/// Data transfer object for StockAdjustmentItem
///
/// This is the public representation of StockAdjustmentItem for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockAdjustmentItemDto {
    pub id: StockAdjustmentItemId,
    pub adjustment_id: Uuid,
    pub provider_id: Uuid,
    pub line_number: i32,
    pub product_id: Uuid,
    pub stock_id: Uuid,
    pub batch_id: Option<Uuid>,
    pub batch_number: Option<String>,
    pub expiry_date: Option<NaiveDate>,
    pub uom: UnitOfMeasure,
    pub system_quantity: Decimal,
    pub counted_quantity: Decimal,
    pub variance_quantity: Decimal,
    pub currency: String,
    pub unit_cost: Decimal,
    pub system_value: Decimal,
    pub adjustment_value: Decimal,
    pub variance_reason: Option<String>,
    pub bin_location: Option<String>,
    pub is_posted: bool,
    pub movement_id: Option<Uuid>,
    pub notes: Option<String>,
    pub data: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Summary view of StockAdjustmentItem for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockAdjustmentItemSummary {
    pub id: StockAdjustmentItemId,
}

/// Reference to StockAdjustmentItem for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockAdjustmentItemRef {
    pub id: StockAdjustmentItemId,
}

// ============================================================================
// STOCKBATCH TYPES
// ============================================================================

/// Type-safe ID for StockBatch
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockBatchId(pub Uuid);

impl StockBatchId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockBatchId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockBatchId> for Uuid {
    fn from(id: StockBatchId) -> Self {
        id.0
    }
}

/// Data transfer object for StockBatch
///
/// This is the public representation of StockBatch for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockBatchDto {
    pub id: StockBatchId,
    pub provider_id: Uuid,
    pub product_id: Uuid,
    pub warehouse_id: Option<Uuid>,
    pub batch_number: String,
    pub supplier_batch: Option<String>,
    pub manufacture_date: Option<NaiveDate>,
    pub received_date: NaiveDate,
    pub expiry_date: Option<NaiveDate>,
    pub initial_quantity: Decimal,
    pub current_quantity: Decimal,
    pub reserved_quantity: Decimal,
    pub quarantine_quantity: Decimal,
    pub uom: String,
    pub unit_cost: Decimal,
    pub total_value: Decimal,
    pub currency: String,
    pub source_type: Option<String>,
    pub source_document: Option<String>,
    pub goods_receipt_id: Option<Uuid>,
    pub supplier_id: Option<Uuid>,
    pub bin_location: Option<String>,
    pub storage_conditions: serde_json::Value,
    pub status: BatchStatus,
    pub is_active: bool,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of StockBatch for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockBatchSummary {
    pub id: StockBatchId,
    pub status: BatchStatus,
}

/// Reference to StockBatch for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockBatchRef {
    pub id: StockBatchId,
}

// ============================================================================
// STOCKMOVEMENT TYPES
// ============================================================================

/// Type-safe ID for StockMovement
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockMovementId(pub Uuid);

impl StockMovementId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockMovementId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockMovementId> for Uuid {
    fn from(id: StockMovementId) -> Self {
        id.0
    }
}

/// Data transfer object for StockMovement
///
/// This is the public representation of StockMovement for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockMovementDto {
    pub id: StockMovementId,
    pub provider_id: Uuid,
    pub movement_number: String,
    pub movement_type: MovementType,
    pub movement_reason: MovementReason,
    pub product_id: Uuid,
    pub warehouse_id: Option<Uuid>,
    pub from_location: Option<String>,
    pub to_location: Option<String>,
    pub quantity: Decimal,
    pub uom: String,
    pub balance_before: Decimal,
    pub balance_after: Decimal,
    pub batch_id: Option<Uuid>,
    pub batch_number: Option<String>,
    pub expiry_date: Option<NaiveDate>,
    pub unit_cost: Decimal,
    pub total_cost: Decimal,
    pub currency: String,
    pub source_type: Option<String>,
    pub source_id: Option<Uuid>,
    pub source_number: Option<String>,
    pub status: MovementStatus,
    pub movement_date: DateTime<Utc>,
    pub posted_at: Option<DateTime<Utc>>,
    pub performed_by: Option<Uuid>,
    pub reason_detail: Option<String>,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of StockMovement for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockMovementSummary {
    pub id: StockMovementId,
    pub status: MovementStatus,
}

/// Reference to StockMovement for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockMovementRef {
    pub id: StockMovementId,
}

// ============================================================================
// STOCKTRANSFER TYPES
// ============================================================================

/// Type-safe ID for StockTransfer
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockTransferId(pub Uuid);

impl StockTransferId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockTransferId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockTransferId> for Uuid {
    fn from(id: StockTransferId) -> Self {
        id.0
    }
}

/// Data transfer object for StockTransfer
///
/// This is the public representation of StockTransfer for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockTransferDto {
    pub id: StockTransferId,
    pub provider_id: Uuid,
    pub transfer_number: String,
    pub transfer_type: TransferType,
    pub from_warehouse_id: Uuid,
    pub from_location: Option<String>,
    pub to_warehouse_id: Uuid,
    pub to_location: Option<String>,
    pub to_provider_id: Option<Uuid>,
    pub transfer_date: NaiveDate,
    pub expected_arrival_date: Option<NaiveDate>,
    pub shipped_at: Option<DateTime<Utc>>,
    pub received_at: Option<DateTime<Utc>>,
    pub total_items: i32,
    pub total_quantity: Decimal,
    pub total_value: Decimal,
    pub currency: String,
    pub shipping_method: Option<String>,
    pub carrier: Option<String>,
    pub tracking_number: Option<String>,
    pub shipping_cost: Option<Decimal>,
    pub status: TransferStatus,
    pub requires_approval: bool,
    pub approved_by: Option<Uuid>,
    pub approved_at: Option<DateTime<Utc>>,
    pub requested_by: Option<Uuid>,
    pub shipped_by: Option<Uuid>,
    pub received_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notes: Option<String>,
    pub receiving_notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of StockTransfer for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockTransferSummary {
    pub id: StockTransferId,
    pub status: TransferStatus,
}

/// Reference to StockTransfer for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockTransferRef {
    pub id: StockTransferId,
}

// ============================================================================
// STOCKTRANSFERITEM TYPES
// ============================================================================

/// Type-safe ID for StockTransferItem
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct StockTransferItemId(pub Uuid);

impl StockTransferItemId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for StockTransferItemId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<StockTransferItemId> for Uuid {
    fn from(id: StockTransferItemId) -> Self {
        id.0
    }
}

/// Data transfer object for StockTransferItem
///
/// This is the public representation of StockTransferItem for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockTransferItemDto {
    pub id: StockTransferItemId,
    pub transfer_id: Uuid,
    pub product_id: Uuid,
    pub requested_quantity: Decimal,
    pub shipped_quantity: Decimal,
    pub received_quantity: Decimal,
    pub rejected_quantity: Decimal,
    pub variance_quantity: Decimal,
    pub uom: String,
    pub batch_id: Option<Uuid>,
    pub batch_number: Option<String>,
    pub expiry_date: Option<NaiveDate>,
    pub unit_cost: Decimal,
    pub total_cost: Decimal,
    pub from_bin: Option<String>,
    pub to_bin: Option<String>,
    pub status: TransferItemStatus,
    pub rejection_reason: Option<String>,
    pub notes: Option<String>,
    pub data: serde_json::Value,
}

/// Summary view of StockTransferItem for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockTransferItemSummary {
    pub id: StockTransferItemId,
    pub status: TransferItemStatus,
}

/// Reference to StockTransferItem for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockTransferItemRef {
    pub id: StockTransferItemId,
}

// ============================================================================
// WAREHOUSE TYPES
// ============================================================================

/// Type-safe ID for Warehouse
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct WarehouseId(pub Uuid);

impl WarehouseId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for WarehouseId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<WarehouseId> for Uuid {
    fn from(id: WarehouseId) -> Self {
        id.0
    }
}

/// Data transfer object for Warehouse
///
/// This is the public representation of Warehouse for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WarehouseDto {
    pub id: WarehouseId,
    pub provider_id: Uuid,
    pub outlet_id: Option<Uuid>,
    pub code: String,
    pub name: String,
    pub description: Option<String>,
    pub warehouse_type: WarehouseType,
    pub is_main: bool,
    pub address_id: Option<Uuid>,
    pub contact_name: Option<String>,
    pub contact_phone: Option<String>,
    pub contact_email: Option<String>,
    pub total_capacity: Option<Decimal>,
    pub capacity_unit: Option<String>,
    pub allow_receipt: bool,
    pub allow_issue: bool,
    pub allow_transfer_in: bool,
    pub allow_transfer_out: bool,
    pub allow_adjustment: bool,
    pub default_inventory_account_id: Option<Uuid>,
    pub status: WarehouseStatus,
    pub notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Summary view of Warehouse for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WarehouseSummary {
    pub id: WarehouseId,
    pub name: String,
    pub contact_name: Option<String>,
    pub status: WarehouseStatus,
}

/// Reference to Warehouse for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WarehouseRef {
    pub id: WarehouseId,
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

// <<< CUSTOM TYPES START >>>
// Add custom public types here
// <<< CUSTOM TYPES END >>>
