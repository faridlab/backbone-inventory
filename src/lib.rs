//! Inventory Module
//!
//! Generated by backbone-schema. Enhanced with runtime implementations.
//!
//! This module provides:
//! - Domain entities and repositories
//! - Application services
//! - HTTP and gRPC handlers
//! - Route configuration
//! - State machine enforcement
//! - Validation rules runtime
//! - RBAC middleware
//! - Trigger execution system
//! - Computed fields
//! - Workflow orchestrator

// Generated modules
pub mod domain;
pub mod infrastructure;
pub mod application;
pub mod presentation;
pub mod seeders;

// Re-exports for convenience - Domain entities
pub use domain::entity::*;

// Re-exports - Infrastructure
pub use infrastructure::persistence::*;

// Re-exports - Application services
pub use application::service::GoodsReceiptService;
pub use application::service::GoodsReceiptItemService;
pub use application::service::ProductCategoryService;
pub use application::service::ProductService;
pub use application::service::PurchaseOrderService;
pub use application::service::PurchaseOrderItemService;
pub use application::service::StockService;
pub use application::service::StockAdjustmentService;
pub use application::service::StockAdjustmentItemService;
pub use application::service::StockBatchService;
pub use application::service::StockMovementService;
pub use application::service::StockTransferService;
pub use application::service::StockTransferItemService;
pub use application::service::WarehouseService;

use std::sync::Arc;
use axum::Router;
use sqlx::PgPool;

/// Inventory module configuration
///
/// Use the builder pattern to configure and register this module:
///
/// ```text
/// let inventory = InventoryModule::builder()
///     .with_database(pool.clone())
///     .build()?;
///
/// let router = inventory.routes();
/// ```
pub struct InventoryModule {
    pub goods_receipt_service: Arc<GoodsReceiptService>,
    pub goods_receipt_item_service: Arc<GoodsReceiptItemService>,
    pub product_category_service: Arc<ProductCategoryService>,
    pub product_service: Arc<ProductService>,
    pub purchase_order_service: Arc<PurchaseOrderService>,
    pub purchase_order_item_service: Arc<PurchaseOrderItemService>,
    pub stock_service: Arc<StockService>,
    pub stock_adjustment_service: Arc<StockAdjustmentService>,
    pub stock_adjustment_item_service: Arc<StockAdjustmentItemService>,
    pub stock_batch_service: Arc<StockBatchService>,
    pub stock_movement_service: Arc<StockMovementService>,
    pub stock_transfer_service: Arc<StockTransferService>,
    pub stock_transfer_item_service: Arc<StockTransferItemService>,
    pub warehouse_service: Arc<WarehouseService>,
}

impl InventoryModule {
    /// Create a new module builder
    pub fn builder() -> InventoryModuleBuilder {
        InventoryModuleBuilder::new()
    }

    /// Configure HTTP routes for this module using Axum
    ///
    /// Returns an Axum Router with all 12 Backbone CRUD endpoints per entity.
    pub fn routes(&self) -> Router {
        use presentation::http::{
            create_goods_receipt_routes,
            create_goods_receipt_item_routes,
            create_product_category_routes,
            create_product_routes,
            create_purchase_order_routes,
            create_purchase_order_item_routes,
            create_stock_routes,
            create_stock_adjustment_routes,
            create_stock_adjustment_item_routes,
            create_stock_batch_routes,
            create_stock_movement_routes,
            create_stock_transfer_routes,
            create_stock_transfer_item_routes,
            create_warehouse_routes,
        };

        Router::new()
            .merge(create_goods_receipt_routes(self.goods_receipt_service.clone()))
            .merge(create_goods_receipt_item_routes(self.goods_receipt_item_service.clone()))
            .merge(create_product_category_routes(self.product_category_service.clone()))
            .merge(create_product_routes(self.product_service.clone()))
            .merge(create_purchase_order_routes(self.purchase_order_service.clone()))
            .merge(create_purchase_order_item_routes(self.purchase_order_item_service.clone()))
            .merge(create_stock_routes(self.stock_service.clone()))
            .merge(create_stock_adjustment_routes(self.stock_adjustment_service.clone()))
            .merge(create_stock_adjustment_item_routes(self.stock_adjustment_item_service.clone()))
            .merge(create_stock_batch_routes(self.stock_batch_service.clone()))
            .merge(create_stock_movement_routes(self.stock_movement_service.clone()))
            .merge(create_stock_transfer_routes(self.stock_transfer_service.clone()))
            .merge(create_stock_transfer_item_routes(self.stock_transfer_item_service.clone()))
            .merge(create_warehouse_routes(self.warehouse_service.clone()))
    }
}

/// Builder for InventoryModule
pub struct InventoryModuleBuilder {
    db_pool: Option<PgPool>,
}

impl InventoryModuleBuilder {
    /// Create a new builder
    pub fn new() -> Self {
        Self {
            db_pool: None,
        }
    }

    /// Set the database connection pool
    pub fn with_database(mut self, pool: PgPool) -> Self {
        self.db_pool = Some(pool);
        self
    }

    /// Build the module with configured dependencies
    pub fn build(self) -> anyhow::Result<InventoryModule> {
        let db_pool = self.db_pool
            .ok_or_else(|| anyhow::anyhow!("Database pool not configured"))?;

        // GoodsReceipt service
        let goods_receipt_repository = Arc::new(GoodsReceiptRepository::new(db_pool.clone()));
        let goods_receipt_service = Arc::new(GoodsReceiptService::with_repository(goods_receipt_repository.clone()));

        // GoodsReceiptItem service
        let goods_receipt_item_repository = Arc::new(GoodsReceiptItemRepository::new(db_pool.clone()));
        let goods_receipt_item_service = Arc::new(GoodsReceiptItemService::with_repository(goods_receipt_item_repository.clone()));

        // ProductCategory service
        let product_category_repository = Arc::new(ProductCategoryRepository::new(db_pool.clone()));
        let product_category_service = Arc::new(ProductCategoryService::with_repository(product_category_repository.clone()));

        // Product service
        let product_repository = Arc::new(ProductRepository::new(db_pool.clone()));
        let product_service = Arc::new(ProductService::with_repository(product_repository.clone()));

        // PurchaseOrder service
        let purchase_order_repository = Arc::new(PurchaseOrderRepository::new(db_pool.clone()));
        let purchase_order_service = Arc::new(PurchaseOrderService::with_repository(purchase_order_repository.clone()));

        // PurchaseOrderItem service
        let purchase_order_item_repository = Arc::new(PurchaseOrderItemRepository::new(db_pool.clone()));
        let purchase_order_item_service = Arc::new(PurchaseOrderItemService::with_repository(purchase_order_item_repository.clone()));

        // Stock service
        let stock_repository = Arc::new(StockRepository::new(db_pool.clone()));
        let stock_service = Arc::new(StockService::with_repository(stock_repository.clone()));

        // StockAdjustment service
        let stock_adjustment_repository = Arc::new(StockAdjustmentRepository::new(db_pool.clone()));
        let stock_adjustment_service = Arc::new(StockAdjustmentService::with_repository(stock_adjustment_repository.clone()));

        // StockAdjustmentItem service
        let stock_adjustment_item_repository = Arc::new(StockAdjustmentItemRepository::new(db_pool.clone()));
        let stock_adjustment_item_service = Arc::new(StockAdjustmentItemService::with_repository(stock_adjustment_item_repository.clone()));

        // StockBatch service
        let stock_batch_repository = Arc::new(StockBatchRepository::new(db_pool.clone()));
        let stock_batch_service = Arc::new(StockBatchService::with_repository(stock_batch_repository.clone()));

        // StockMovement service
        let stock_movement_repository = Arc::new(StockMovementRepository::new(db_pool.clone()));
        let stock_movement_service = Arc::new(StockMovementService::with_repository(stock_movement_repository.clone()));

        // StockTransfer service
        let stock_transfer_repository = Arc::new(StockTransferRepository::new(db_pool.clone()));
        let stock_transfer_service = Arc::new(StockTransferService::with_repository(stock_transfer_repository.clone()));

        // StockTransferItem service
        let stock_transfer_item_repository = Arc::new(StockTransferItemRepository::new(db_pool.clone()));
        let stock_transfer_item_service = Arc::new(StockTransferItemService::with_repository(stock_transfer_item_repository.clone()));

        // Warehouse service
        let warehouse_repository = Arc::new(WarehouseRepository::new(db_pool.clone()));
        let warehouse_service = Arc::new(WarehouseService::with_repository(warehouse_repository.clone()));

        // <<< CUSTOM
        // END CUSTOM

        Ok(InventoryModule {
            goods_receipt_service,
            goods_receipt_item_service,
            product_category_service,
            product_service,
            purchase_order_service,
            purchase_order_item_service,
            stock_service,
            stock_adjustment_service,
            stock_adjustment_item_service,
            stock_batch_service,
            stock_movement_service,
            stock_transfer_service,
            stock_transfer_item_service,
            warehouse_service,
            // <<< CUSTOM
            // END CUSTOM
        })
    }
}

impl Default for InventoryModuleBuilder {
    fn default() -> Self {
        Self::new()
    }
}
