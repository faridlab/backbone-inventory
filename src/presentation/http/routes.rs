//! HTTP route configuration
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides consolidated Axum route configuration using backbone-core's
//! BackboneCrudHandler for all 12 standard Backbone CRUD endpoints per entity.

use axum::Router;
use std::sync::Arc;

use super::{
    goods_receipt_handler::create_goods_receipt_routes,
    goods_receipt_item_handler::create_goods_receipt_item_routes,
    product_category_handler::create_product_category_routes,
    product_handler::create_product_routes,
    purchase_order_handler::create_purchase_order_routes,
    purchase_order_item_handler::create_purchase_order_item_routes,
    stock_handler::create_stock_routes,
    stock_adjustment_handler::create_stock_adjustment_routes,
    stock_adjustment_item_handler::create_stock_adjustment_item_routes,
    stock_batch_handler::create_stock_batch_routes,
    stock_movement_handler::create_stock_movement_routes,
    stock_transfer_handler::create_stock_transfer_routes,
    stock_transfer_item_handler::create_stock_transfer_item_routes,
    warehouse_handler::create_warehouse_routes,
};

use crate::application::service::{
    GoodsReceiptService,
    GoodsReceiptItemService,
    ProductCategoryService,
    ProductService,
    PurchaseOrderService,
    PurchaseOrderItemService,
    StockService,
    StockAdjustmentService,
    StockAdjustmentItemService,
    StockBatchService,
    StockMovementService,
    StockTransferService,
    StockTransferItemService,
    WarehouseService,
};

/// Services collection for all CRUD endpoints
pub struct HttpServices {
    pub goods_receipt: Arc<GoodsReceiptService>,
    pub goods_receipt_item: Arc<GoodsReceiptItemService>,
    pub product_category: Arc<ProductCategoryService>,
    pub product: Arc<ProductService>,
    pub purchase_order: Arc<PurchaseOrderService>,
    pub purchase_order_item: Arc<PurchaseOrderItemService>,
    pub stock: Arc<StockService>,
    pub stock_adjustment: Arc<StockAdjustmentService>,
    pub stock_adjustment_item: Arc<StockAdjustmentItemService>,
    pub stock_batch: Arc<StockBatchService>,
    pub stock_movement: Arc<StockMovementService>,
    pub stock_transfer: Arc<StockTransferService>,
    pub stock_transfer_item: Arc<StockTransferItemService>,
    pub warehouse: Arc<WarehouseService>,
}

/// Configure all HTTP routes for this module using Axum and BackboneCrudHandler.
///
/// Each entity gets 12 standard Backbone CRUD endpoints:
/// 1. GET /api/v1/{collection} - List with pagination
/// 2. POST /api/v1/{collection} - Create
/// 3. GET /api/v1/{collection}/:id - Get by ID
/// 4. PUT /api/v1/{collection}/:id - Full update
/// 5. PATCH /api/v1/{collection}/:id - Partial update
/// 6. DELETE /api/v1/{collection}/:id - Soft delete
/// 7. POST /api/v1/{collection}/bulk - Bulk create
/// 8. POST /api/v1/{collection}/upsert - Upsert
/// 9. GET /api/v1/{collection}/trash - List deleted
/// 10. POST /api/v1/{collection}/:id/restore - Restore
/// 11. DELETE /api/v1/{collection}/empty - Empty trash
/// 12. GET /api/v1/{collection}/:id/deleted - Get deleted by ID
pub fn configure_routes(services: HttpServices) -> Router {
    Router::new()
        // GoodsReceipt routes (12 Backbone endpoints)
        .merge(create_goods_receipt_routes(services.goods_receipt))
        // GoodsReceiptItem routes (12 Backbone endpoints)
        .merge(create_goods_receipt_item_routes(services.goods_receipt_item))
        // ProductCategory routes (12 Backbone endpoints)
        .merge(create_product_category_routes(services.product_category))
        // Product routes (12 Backbone endpoints)
        .merge(create_product_routes(services.product))
        // PurchaseOrder routes (12 Backbone endpoints)
        .merge(create_purchase_order_routes(services.purchase_order))
        // PurchaseOrderItem routes (12 Backbone endpoints)
        .merge(create_purchase_order_item_routes(services.purchase_order_item))
        // Stock routes (12 Backbone endpoints)
        .merge(create_stock_routes(services.stock))
        // StockAdjustment routes (12 Backbone endpoints)
        .merge(create_stock_adjustment_routes(services.stock_adjustment))
        // StockAdjustmentItem routes (12 Backbone endpoints)
        .merge(create_stock_adjustment_item_routes(services.stock_adjustment_item))
        // StockBatch routes (12 Backbone endpoints)
        .merge(create_stock_batch_routes(services.stock_batch))
        // StockMovement routes (12 Backbone endpoints)
        .merge(create_stock_movement_routes(services.stock_movement))
        // StockTransfer routes (12 Backbone endpoints)
        .merge(create_stock_transfer_routes(services.stock_transfer))
        // StockTransferItem routes (12 Backbone endpoints)
        .merge(create_stock_transfer_item_routes(services.stock_transfer_item))
        // Warehouse routes (12 Backbone endpoints)
        .merge(create_warehouse_routes(services.warehouse))
}

/// Create an individual entity's routes (for modular configuration)
pub mod individual {
    use super::*;

    pub fn goods_receipt_routes(service: Arc<GoodsReceiptService>) -> Router {
        create_goods_receipt_routes(service)
    }

    pub fn goods_receipt_item_routes(service: Arc<GoodsReceiptItemService>) -> Router {
        create_goods_receipt_item_routes(service)
    }

    pub fn product_category_routes(service: Arc<ProductCategoryService>) -> Router {
        create_product_category_routes(service)
    }

    pub fn product_routes(service: Arc<ProductService>) -> Router {
        create_product_routes(service)
    }

    pub fn purchase_order_routes(service: Arc<PurchaseOrderService>) -> Router {
        create_purchase_order_routes(service)
    }

    pub fn purchase_order_item_routes(service: Arc<PurchaseOrderItemService>) -> Router {
        create_purchase_order_item_routes(service)
    }

    pub fn stock_routes(service: Arc<StockService>) -> Router {
        create_stock_routes(service)
    }

    pub fn stock_adjustment_routes(service: Arc<StockAdjustmentService>) -> Router {
        create_stock_adjustment_routes(service)
    }

    pub fn stock_adjustment_item_routes(service: Arc<StockAdjustmentItemService>) -> Router {
        create_stock_adjustment_item_routes(service)
    }

    pub fn stock_batch_routes(service: Arc<StockBatchService>) -> Router {
        create_stock_batch_routes(service)
    }

    pub fn stock_movement_routes(service: Arc<StockMovementService>) -> Router {
        create_stock_movement_routes(service)
    }

    pub fn stock_transfer_routes(service: Arc<StockTransferService>) -> Router {
        create_stock_transfer_routes(service)
    }

    pub fn stock_transfer_item_routes(service: Arc<StockTransferItemService>) -> Router {
        create_stock_transfer_item_routes(service)
    }

    pub fn warehouse_routes(service: Arc<WarehouseService>) -> Router {
        create_warehouse_routes(service)
    }

}
