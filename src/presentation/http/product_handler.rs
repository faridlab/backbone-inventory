//! Product REST handlers
//!
//! Auto-generated by backbone-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::{AuthContext, AuthMiddleware};

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{ProductService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateProductDto, UpdateProductDto, PatchProductDto, ProductResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum ProductError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for ProductError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for ProductError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "PRODUCT_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "PRODUCT_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PRODUCT_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PRODUCT_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for Product.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /products | List with pagination |
/// | POST | /products | Create new |
/// | GET | /products/:id | Get by ID |
/// | PUT | /products/:id | Full update |
/// | PATCH | /products/:id | Partial update |
/// | DELETE | /products/:id | Soft delete |
/// | POST | /products/bulk | Bulk create |
/// | POST | /products/upsert | Upsert |
/// | GET | /products/trash | List deleted |
/// | POST | /products/:id/restore | Restore |
/// | DELETE | /products/empty | Empty trash |
/// | GET | /products/:id/deleted | Get deleted by ID |
/// | DELETE | /products/trash/:id | Permanent delete from trash |
/// | GET | /products/count | Count active entities |
/// | GET | /products/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(ProductService::with_repository(repository));
/// let router = create_product_routes(service);
/// ```
pub fn create_product_routes(service: Arc<ProductService>) -> Router {
    BackboneCrudHandler::<ProductService, Product, CreateProductDto, UpdateProductDto, ProductResponseDto>::routes(
        service,
        "/products",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_product_routes<A: AuthMiddleware + 'static>(
    service: Arc<ProductService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_product_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                match auth.authenticate(&req).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

