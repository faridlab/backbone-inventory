//! PurchaseReceiptItem API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseReceiptItem CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASERECEIPTITEM TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseReceiptItem
pub struct PurchaseReceiptItemTestData;

impl TestDataGenerator for PurchaseReceiptItemTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "receipt_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "quantity": 0,
            "rate": 0,
            "amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "receipt_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "quantity": 0,
            "rate": 0,
            "amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/purchase_receipts", &super::purchase_receipt_api_test::PurchaseReceiptTestData).await {
            deps.push(("receipt_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// PURCHASERECEIPTITEM API TEST
// ============================================================================

/// PurchaseReceiptItem API test suite
pub struct PurchaseReceiptItemApiTest {
    inner: GenericCrudTest<PurchaseReceiptItemTestData>,
}

impl PurchaseReceiptItemApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_receipt_items", "PurchaseReceiptItem");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseReceiptItemTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseReceiptItemApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_receipt_item_crud() {
        let mut test = PurchaseReceiptItemApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseReceiptItem specific tests here
// <<< CUSTOM TESTS END >>>
