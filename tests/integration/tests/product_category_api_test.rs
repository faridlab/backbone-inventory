//! ProductCategory API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the ProductCategory CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PRODUCTCATEGORY TEST DATA GENERATOR
// ============================================================================

/// Test data generator for ProductCategory
pub struct ProductCategoryTestData;

impl TestDataGenerator for ProductCategoryTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "provider_id": null,
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "description": "Test description",
            "parent_id": null,
            "level": 1,
            "path": null,
            "sort_order": 1,
            "default_uom": null,
            "default_costing_method": null,
            "default_inventory_account_id": null,
            "default_cogs_account_id": null,
            "is_active": true,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "provider_id": null,
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "description": "Test description",
            "parent_id": null,
            "level": 1,
            "path": null,
            "sort_order": 1,
            "default_uom": null,
            "default_costing_method": null,
            "default_inventory_account_id": null,
            "default_cogs_account_id": null,
            "is_active": true,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PRODUCTCATEGORY API TEST
// ============================================================================

/// ProductCategory API test suite
pub struct ProductCategoryApiTest {
    inner: GenericCrudTest<ProductCategoryTestData>,
}

impl ProductCategoryApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/product_categories", "ProductCategory");
        Self {
            inner: GenericCrudTest::new(config, ProductCategoryTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for ProductCategoryApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_product_category_crud() {
        let mut test = ProductCategoryApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add ProductCategory specific tests here
// <<< CUSTOM TESTS END >>>
