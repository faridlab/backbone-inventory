//! PurchaseReceipt API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the PurchaseReceipt CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PURCHASERECEIPT TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PurchaseReceipt
pub struct PurchaseReceiptTestData;

impl TestDataGenerator for PurchaseReceiptTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "receipt_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "supplier_id": Uuid::new_v4().to_string(),
            "source_po_id": null,
            "warehouse_id": Uuid::new_v4().to_string(),
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "total_value": 0,
            "inventory_account_id": Uuid::new_v4().to_string(),
            "grir_account_id": Uuid::new_v4().to_string(),
            "status": "draft",
            "posting_state": "pending",
            "journal_id": null,
            "accounting_post_id": null,
            "posted_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "receipt_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "company_id": Uuid::new_v4().to_string(),
            "branch_id": null,
            "supplier_id": Uuid::new_v4().to_string(),
            "source_po_id": null,
            "warehouse_id": Uuid::new_v4().to_string(),
            "posting_date": Utc::now().format("%Y-%m-%d").to_string(),
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "total_value": 0,
            "inventory_account_id": Uuid::new_v4().to_string(),
            "grir_account_id": Uuid::new_v4().to_string(),
            "status": "draft",
            "posting_state": "pending",
            "journal_id": null,
            "accounting_post_id": null,
            "posted_at": now,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/warehouses", &super::warehouse_api_test::WarehouseTestData).await {
            deps.push(("warehouse_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// PURCHASERECEIPT API TEST
// ============================================================================

/// PurchaseReceipt API test suite
pub struct PurchaseReceiptApiTest {
    inner: GenericCrudTest<PurchaseReceiptTestData>,
}

impl PurchaseReceiptApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/purchase_receipts", "PurchaseReceipt");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, PurchaseReceiptTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PurchaseReceiptApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_purchase_receipt_crud() {
        let mut test = PurchaseReceiptApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PurchaseReceipt specific tests here
// <<< CUSTOM TESTS END >>>
