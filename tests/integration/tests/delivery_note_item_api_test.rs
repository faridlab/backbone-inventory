//! DeliveryNoteItem API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the DeliveryNoteItem CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// DELIVERYNOTEITEM TEST DATA GENERATOR
// ============================================================================

/// Test data generator for DeliveryNoteItem
pub struct DeliveryNoteItemTestData;

impl TestDataGenerator for DeliveryNoteItemTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "delivery_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "quantity": 0,
            "valuation_rate": 0,
            "cogs_amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "delivery_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "quantity": 0,
            "valuation_rate": 0,
            "cogs_amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/delivery_notes", &super::delivery_note_api_test::DeliveryNoteTestData).await {
            deps.push(("delivery_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// DELIVERYNOTEITEM API TEST
// ============================================================================

/// DeliveryNoteItem API test suite
pub struct DeliveryNoteItemApiTest {
    inner: GenericCrudTest<DeliveryNoteItemTestData>,
}

impl DeliveryNoteItemApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/delivery_note_items", "DeliveryNoteItem");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, DeliveryNoteItemTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for DeliveryNoteItemApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_delivery_note_item_crud() {
        let mut test = DeliveryNoteItemApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add DeliveryNoteItem specific tests here
// <<< CUSTOM TESTS END >>>
