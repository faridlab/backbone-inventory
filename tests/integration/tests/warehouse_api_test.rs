//! Warehouse API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the Warehouse CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// WAREHOUSE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for Warehouse
pub struct WarehouseTestData;

impl TestDataGenerator for WarehouseTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "provider_id": null,
            "outlet_id": null,
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "description": "Test description",
            "warehouse_type": null,
            "is_main": false,
            "address_id": null,
            "contact_name": null,
            "contact_phone": null,
            "contact_email": null,
            "total_capacity": null,
            "capacity_unit": null,
            "allow_receipt": null,
            "allow_issue": null,
            "allow_transfer_in": null,
            "allow_transfer_out": null,
            "allow_adjustment": null,
            "default_inventory_account_id": null,
            "status": null,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "provider_id": null,
            "outlet_id": null,
            "code": format!("TEST_{}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "description": "Test description",
            "warehouse_type": null,
            "is_main": false,
            "address_id": null,
            "contact_name": null,
            "contact_phone": null,
            "contact_email": null,
            "total_capacity": null,
            "capacity_unit": null,
            "allow_receipt": null,
            "allow_issue": null,
            "allow_transfer_in": null,
            "allow_transfer_out": null,
            "allow_adjustment": null,
            "default_inventory_account_id": null,
            "status": null,
            "notes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// WAREHOUSE API TEST
// ============================================================================

/// Warehouse API test suite
pub struct WarehouseApiTest {
    inner: GenericCrudTest<WarehouseTestData>,
}

impl WarehouseApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/warehouses", "Warehouse");
        Self {
            inner: GenericCrudTest::new(config, WarehouseTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for WarehouseApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_warehouse_crud() {
        let mut test = WarehouseApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add Warehouse specific tests here
// <<< CUSTOM TESTS END >>>
