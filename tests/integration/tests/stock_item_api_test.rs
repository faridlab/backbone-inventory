//! StockItem API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the StockItem CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// STOCKITEM TEST DATA GENERATOR
// ============================================================================

/// Test data generator for StockItem
pub struct StockItemTestData;

impl TestDataGenerator for StockItemTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "stock_uom": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "is_stock_item": false,
            "has_batch": false,
            "valuation_method": "moving_average",
            "reorder_level": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "item_id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "stock_uom": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "is_stock_item": false,
            "has_batch": false,
            "valuation_method": "moving_average",
            "reorder_level": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// STOCKITEM API TEST
// ============================================================================

/// StockItem API test suite
pub struct StockItemApiTest {
    inner: GenericCrudTest<StockItemTestData>,
}

impl StockItemApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/stock_items", "StockItem");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, StockItemTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for StockItemApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_stock_item_crud() {
        let mut test = StockItemApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add StockItem specific tests here
// <<< CUSTOM TESTS END >>>
