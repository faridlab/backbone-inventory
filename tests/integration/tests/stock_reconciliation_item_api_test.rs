//! StockReconciliationItem API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the StockReconciliationItem CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// STOCKRECONCILIATIONITEM TEST DATA GENERATOR
// ============================================================================

/// Test data generator for StockReconciliationItem
pub struct StockReconciliationItemTestData;

impl TestDataGenerator for StockReconciliationItemTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "reconciliation_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "counted_qty": 0,
            "counted_rate": 0,
            "qty_difference": 0,
            "value_difference": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "reconciliation_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "counted_qty": 0,
            "counted_rate": 0,
            "qty_difference": 0,
            "value_difference": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/stock_reconciliations", &super::stock_reconciliation_api_test::StockReconciliationTestData).await {
            deps.push(("reconciliation_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// STOCKRECONCILIATIONITEM API TEST
// ============================================================================

/// StockReconciliationItem API test suite
pub struct StockReconciliationItemApiTest {
    inner: GenericCrudTest<StockReconciliationItemTestData>,
}

impl StockReconciliationItemApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/stock_reconciliation_items", "StockReconciliationItem");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, StockReconciliationItemTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for StockReconciliationItemApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_stock_reconciliation_item_crud() {
        let mut test = StockReconciliationItemApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add StockReconciliationItem specific tests here
// <<< CUSTOM TESTS END >>>
