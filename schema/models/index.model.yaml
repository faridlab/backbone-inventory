# =============================================================================
# inventory Module - Schema Index
# =============================================================================
# Version: 2.0
# Description: Module schema index. `module:` and `schema:` are stamped from the
# module name by `metaphor module create`. `schema:` gives this module its OWN
# Postgres schema (migrations emit `CREATE SCHEMA inventory` and qualify tables
# as `inventory.<table>`) — the convention every backbone module follows
# (see backbone-sapiens, backbone-bucket). Do not remove it.
# =============================================================================

module: inventory
version: 2
schema: inventory

# -----------------------------------------------------------------------------
# Tenancy: none, by design (ADR-0029) — this module is tenant-agnostic and
# ships no fence of its own. The composing service owns tenancy: its decorator
# arms RLS (ENABLE + FORCE) with zero module policies and stamps the acting
# org unit via the fill trigger; scope visibility rides the ambient org scope.
#
# Shared masters (Location, Lot, Package, Route, RouteRule, OperationType,
# ScrapReasonTag, and the storage family: PackageType, StorageCategory,
# StorageCategoryCapacity, PutawayRule) anchor at the tenant ROOT org node
# (allow_root) — every entitled scope sees them through the scope union
# (subtree ∪ tenant root). Transactions and per-unit configuration rows
# (Warehouse, StockItem, Transfer, moves/lines, Quant, ReorderingRule, Bin,
# StockLedgerEntry, vouchers, InventoryCompanySetting, LandedCost, picking
# batches, scraps) are plain org-scoped rows.
#
# Company-scoped uniqueness now lives in the composing decorator as org-scoped
# re-declarations (13): warehouses(org, code) and (org, name),
# stock_items(org, item), lots(name, item, org), locations(barcode, org),
# transfers(name, org), reordering_rules(item, location, org),
# bins(org, item, warehouse), inventory_company_settings(org),
# landed_costs(org, lc_number), picking_batches(name, org), scraps(name, org),
# scrap_reason_tags(name, org).
# -----------------------------------------------------------------------------

description: "Inventory: quant-driven stock (one quant per item x location x lot x package x owner; reserved_quantity authoritative) + 7-state stock moves projected onto transfer documents + append-only Stock Ledger (SLE) with moving-average valuation. Vouchers (PurchaseReceipt / DeliveryNote / StockEntry / StockReconciliation) mint Transfers grouping moves; move _action_done writes the SLE rows and GL legs (Dr Inventory - Cr GR/IR on receipt, Dr COGS - Cr Inventory on delivery) into backbone-accounting via the GL-posting contract."

config:
  database: postgresql
  soft_delete: true
  audit: true
  default_timestamps: true
  generators:
    disabled:
      - graphql
      - grpc
      - proto

# Reuse sapiens for user identity (actors) — logical refs, no DB constraint.
external_imports:
  - module: sapiens
    types: [User]

# Shared value-object types available to all models in this module.
shared_types:
  Timestamps:
    created_at:
      type: datetime
      attributes: ["@default(now)"]
      description: "Record creation timestamp"
    updated_at:
      type: datetime
      attributes: ["@updated_at"]
      description: "Last update timestamp"
    deleted_at:
      type: datetime?
      description: "Soft delete timestamp"

  Actors:
    created_by:
      type: uuid?
      attributes: ["@foreign_key(sapiens.User.id)"]
      description: "User who created this record"
    updated_by:
      type: uuid?
      attributes: ["@foreign_key(sapiens.User.id)"]
      description: "User who last updated this record"
    deleted_by:
      type: uuid?
      attributes: ["@foreign_key(sapiens.User.id)"]
      description: "User who deleted this record"

  Metadata: [Timestamps, Actors]

# Import each entity model (parents before children).
imports:
  - warehouse.model.yaml
  - location.model.yaml
  - tracking.model.yaml
  - operation.model.yaml
  - procurement.model.yaml
  - move.model.yaml
  - quant.model.yaml
  - stock_ledger.model.yaml
  - stock_entry.model.yaml
  - purchase_receipt.model.yaml
  - delivery_note.model.yaml
  - stock_reconciliation.model.yaml
  - inventory_company_setting.model.yaml
  - landed_cost.model.yaml
